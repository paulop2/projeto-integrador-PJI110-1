---
phase: 06-dashboard-e-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - backend/tests/test_admin.py
  - backend/tests/test_professor.py
autonomous: false
requirements:
  - DASH-01

must_haves:
  truths:
    - Full backend test suite passes with zero failures
    - Frontend TypeScript build succeeds with zero errors
    - Human verifies dashboard functionality in browser
  artifacts:
    - path: "backend/tests/test_admin.py"
      provides: "Green dashboard tests"
    - path: "backend/tests/test_professor.py"
      provides: "Green professor metrics tests"
  key_links:
    - from: "verification script"
      to: "pytest + npm run build"
      via: "automated commands"
---

<objective>
Verify all Phase 6 deliverables through automated tests and human browser verification.

Purpose: Confirm the dashboard and polish features work end-to-end before marking the phase complete.
Output: Green test suite, clean build, and human sign-off.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-e-polish/06-CONTEXT.md
@.planning/phases/06-dashboard-e-polish/06-01-SUMMARY.md
@.planning/phases/06-dashboard-e-polish/06-02-SUMMARY.md
@.planning/phases/06-dashboard-e-polish/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification</name>
  <files>backend/tests/test_admin.py, backend/tests/test_professor.py</files>
  <action>
    1. Run the complete backend test suite:
       ```bash
       cd backend && python -m pytest tests/ -x -q
       ```
       If any test fails, diagnose and fix the issue. Common issues:
       - Missing imports in test files
       - Schema validation errors due to wrong field names
       - Database fixture setup issues (ensure Avaliacao, Nota, Chamada, Presenca rows are created in tests)
    
    2. Run the frontend TypeScript build:
       ```bash
       cd frontend && npm run build
       ```
       If any TypeScript error appears, fix it. Common issues:
       - Missing type imports
       - Optional field access without null checks
       - JSX syntax errors
    
    3. If fixes are required, commit them atomically with message: `fix(06-04): address verification failures`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -x -q && cd ../frontend && npm run build</automated>
  </verify>
  <done>
    - `pytest tests/` returns 0 failures
    - `npm run build` exits with code 0
    - No console warnings about missing dependencies or type errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification</name>
  <what-built>
    Phase 6 dashboard and polish features:
    1. Admin dashboard with count cards + alert card + per-turma performance table
    2. Professor landing page with TurmaCards showing media geral and % aprovados
    3. Global toast error handling for API failures
    4. Skeleton loading states on all data-heavy pages
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd backend && uvicorn src.main:app --reload`
    2. Start the frontend: `cd frontend && npm run dev`
    3. Open http://localhost:5173 and login as admin

    **Admin dashboard verification:**
    4. Navigate to /admin (dashboard)
    5. Confirm count cards still display (Alunos, Turmas, Disciplinas, Professores, Responsáveis)
    6. Below count cards, confirm:
       - Alert card appears (green if no risk, yellow if alunos em risco > 0)
       - Performance table shows: Turma | Alunos | Média Geral | % Aprovados | Status
       - Status column shows green "Aprovado" or red "Reprovado" badges
    7. If there are no turmas or no data, confirm friendly empty state message

    **Professor portal verification:**
    8. Logout and login as professor
    9. Navigate to /professor
    10. Confirm TurmaCards show:
        - Turma name, disciplinas, num_alunos
        - NEW: "Média geral: X,X" and "% Aprovados: XX%"
    11. Click a TurmaCard — confirm navigation to turma details still works

    **Error toast verification:**
    12. Stop the backend server (Ctrl+C)
    13. In the frontend, try navigating to a different page
    14. Confirm a red toast appears with message like "Erro no servidor. Tente novamente em instantes."
    15. Restart the backend

    **Skeleton loading verification:**
    16. Open browser DevTools → Network → set throttling to "Slow 3G"
    17. Refresh the admin dashboard
    18. Confirm skeleton placeholders (gray pulsing blocks) appear instead of "Carregando..." text
    19. Restore network speed
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification phase — no new code or trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | None | Verification | accept | This plan only runs tests and human checks; no security changes |
</threat_model>

<verification>
- Automated: `cd backend && python -m pytest tests/ -x -q` passes
- Automated: `cd frontend && npm run build` passes
- Human: Browser verification checklist completed and signed off
</verification>

<success_criteria>
- Backend test suite: 0 failures
- Frontend build: 0 TypeScript errors
- Human verification: All checklist items confirmed working
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-e-polish/06-04-SUMMARY.md`
</output>
