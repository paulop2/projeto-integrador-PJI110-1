---
phase: 04-portal-do-professor
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - .planning/phases/04-portal-do-professor/04-04-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05

must_haves:
  truths:
    - "Full pytest suite (tests/) exits 0 — Phase 3 + Phase 4 tests all green"
    - "TypeScript build (npx tsc --noEmit) exits 0"
    - "Production frontend build (npm run build) succeeds"
    - "Human confirms professor portal works end-to-end in browser: landing page, chamada tab, notas tab, frequencia tab"
  artifacts:
    - path: "backend/tests/test_professor.py"
      provides: "All tests passing (green)"
      contains: "def test_frequencia_aggregation"
    - path: ".planning/phases/04-portal-do-professor/04-04-SUMMARY.md"
      provides: "Phase 4 completion summary"
      contains: "Phase 4"
  key_links:
    - from: "tests/test_professor.py"
      to: "backend/src/professor/service.py"
      via: "pytest assertions"
      pattern: "pytest tests/test_professor\\.py"
---

<objective>
Phase 4 verification: run the full automated test suite, run TypeScript and build checks, then verify the professor portal manually in a browser. This plan is the quality gate before marking Phase 4 complete.

Purpose: Confirm that all PROF-01 through PROF-05 requirements are satisfied and no Phase 3 regressions exist.
Output: Green test suite, clean build, human-verified browser flow, updated STATE.md and ROADMAP.md.
</objective>

<execution_context>
@/Users/pvs/projetos/projeto-integrador-PJI110-1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pvs/projetos/projeto-integrador-PJI110-1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portal-do-professor/04-VALIDATION.md
@.planning/phases/04-portal-do-professor/04-02-SUMMARY.md
@.planning/phases/04-portal-do-professor/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification (backend tests + frontend build)</name>
  <files>
    .planning/phases/04-portal-do-professor/04-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/04-portal-do-professor/04-VALIDATION.md — verification map, expected test names and commands
    - backend/tests/test_professor.py — all test function names to confirm coverage
    - backend/src/professor/service.py — confirm no NotImplementedError remains
  </read_first>
  <action>
Run verification commands in sequence. Fix any failures before proceeding to the checkpoint.

**Step 1: Backend test suite**
```bash
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/backend
python -m pytest tests/test_professor.py -v -q
```
Expected: all test_professor.py tests pass. If any test fails, read the failure output, fix the service code, and re-run.

Then run full suite (Phase 3 regression check):
```bash
python -m pytest tests/ -q
```
Expected: all tests pass (exit code 0). If Phase 3 tests fail, investigate imports — likely the new ORM models were imported in main.py but cause a metadata conflict. Fix in models/__init__.py if needed.

**Step 2: TypeScript type check**
```bash
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend
npx tsc --noEmit
```
Expected: 0 errors. Fix any type errors in professor pages/components before continuing.

**Step 3: Production build**
```bash
npm run build
```
Expected: build succeeds (dist/ created, no errors). Fix any build errors before continuing.

**Step 4: Static structural checks (verify spec compliance)**
Run these grep checks and confirm expected output:

```bash
# Verify no ProfessorDashboard stub remains in App.tsx
grep -c "ProfessorDashboard" /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend/src/App.tsx
# Expected: 0

# Verify professor routes exist
grep -c "ProfessorLandingPage" /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend/src/App.tsx
# Expected: >= 2 (import + route)

# Verify GradeTable does NOT use react-hook-form
grep -c "useForm" /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend/src/components/professor/GradeTable.tsx
# Expected: 0

# Verify ownership check in service
grep -c "_assert_professor_owns_turma" /Users/pvs/projetos/projeto-integrador-PJI110-1/backend/src/professor/service.py
# Expected: >= 4 (definition + called in get_chamada, upsert_chamada, get_notas, upsert_notas, get_frequencia)

# Verify delete-replace pattern
grep -c "synchronize_session=False" /Users/pvs/projetos/projeto-integrador-PJI110-1/backend/src/professor/service.py
# Expected: >= 1

# Verify no NotImplementedError remains
grep -c "NotImplementedError" /Users/pvs/projetos/projeto-integrador-PJI110-1/backend/src/professor/service.py
# Expected: 0

# Verify frequencia at-risk highlight in FrequencyTable
grep -c "bg-red-50" /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend/src/components/professor/FrequencyTable.tsx
# Expected: >= 1

# Verify professor router is registered in main.py
grep -c "professor_router" /Users/pvs/projetos/projeto-integrador-PJI110-1/backend/src/main.py
# Expected: 2 (import + include_router)
```

If all checks pass, record results and proceed to Task 2 (human verification checkpoint).

If any check fails, fix the root cause before writing the summary or advancing.

After all checks pass, update:
- `.planning/ROADMAP.md`: change Phase 4 Plans line from `TBD` to `4 plans`, add plan list with checkboxes (all checked)
- `.planning/STATE.md`: update Current Position to Phase 4 complete, update Last activity date, add completed Phase 4 entry to performance table
  </action>
  <verify>
    <automated>cd /Users/pvs/projetos/projeto-integrador-PJI110-1/backend && python -m pytest tests/ -q && cd /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_professor.py -q` exits 0 (all professor tests green)
    - `python -m pytest tests/ -q` exits 0 (no Phase 3 regressions)
    - `npx tsc --noEmit` exits 0 (0 TypeScript errors)
    - `npm run build` exits 0 (production build succeeds)
    - `grep -c "NotImplementedError" backend/src/professor/service.py` outputs `0`
    - `grep -c "synchronize_session=False" backend/src/professor/service.py` outputs at least `1`
    - `grep -c "ProfessorDashboard" frontend/src/App.tsx` outputs `0`
    - `grep -c "professor_router" backend/src/main.py` outputs `2`
  </acceptance_criteria>
  <done>All automated checks pass. ROADMAP.md and STATE.md updated. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Professor portal: ProfessorLandingPage (turma cards), ProfessorTurmaPage (Chamada / Notas / Frequencia tabs), all supporting components (TurmaCard, TabNav, AttendanceToggle, GradeTable, FrequencyTable). Backend: professor module with 6 API endpoints, full service logic, 8+ tests green.
  </what-built>
  <how-to-verify>
Start both servers:
```bash
# Terminal 1:
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/backend
uvicorn src.main:app --reload

# Terminal 2:
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend
npm run dev
```

**Pre-condition: create test data via admin panel first (or use API directly).**

If you do not yet have a professor linked to a turma with students, log in as admin and create:
1. A professor account (Professores page)
2. A turma with that professor linked to a disciplina (Turmas page)
3. Two or three alunos enrolled in that turma (Alunos page)

**Then log in as the professor:**
- Open http://localhost:5173
- Log in with the professor account credentials

**Verification checklist:**

**PROF-02 — Landing page (turma cards):**
- [ ] Landing page shows "Minhas Turmas" heading
- [ ] Cards appear for each turma linked to this professor (not turmas of other professors)
- [ ] Each card shows: turma name, disciplina name(s), number of alunos
- [ ] Clicking a card navigates to /professor/turmas/:id

**PROF-01 — Chamada tab:**
- [ ] Breadcrumb shows "Minhas Turmas / [Turma Nome]"
- [ ] Default date in date input is today's date
- [ ] Student list shows all enrolled alunos
- [ ] Each student row has AttendanceToggle — default Presente (green)
- [ ] Clicking "Falta" button on a student shows red active state; "Presente" deactivates
- [ ] Click "Salvar chamada" — sonner toast "Chamada salva com sucesso." appears

**PROF-04 — Edit existing chamada:**
- [ ] Navigate back to Chamada tab with the same date — yellow warning banner appears: "⚠ Chamada ja registrada nesta data. Editar ira sobrescrever os registros anteriores."
- [ ] Change one student's status; click "Salvar chamada" — ConfirmDialog "Sobrescrever chamada" appears
- [ ] Click Confirm — toast appears; previous record replaced

**PROF-03 — Notas tab:**
- [ ] Click "Notas" tab — GradeTable appears with alunos in rows, 1o–4o Bimestre in columns
- [ ] Enter a valid value (e.g., 8.5) in a cell — no red border
- [ ] Enter an invalid value (e.g., "abc" or "15") — cell gets red border; Save button disabled
- [ ] Fix invalid value; click "Salvar notas" — toast "Notas salvas com sucesso."
- [ ] Navigate away and back to Notas tab — saved values appear in cells

**PROF-04 — Edit nota:**
- [ ] Change a value and save again — same cell updates (no duplicate rows in DB)

**PROF-05 — Frequencia tab:**
- [ ] Click "Frequencia" tab — table appears with Aluno / Presenca / Aulas columns
- [ ] Percentual is shown (e.g., "100%" or "50%")
- [ ] Count shown (e.g., "1/1 aulas" or "1/2 aulas")
- [ ] If any aluno is below 75%: row has red background; "⚠ Abaixo de 75%" badge visible
- [ ] Info banner appears: "Alunos com frequencia abaixo de 75%..."
  </how-to-verify>
  <resume-signal>
Type "approved" if all checklist items pass. Describe any failures to trigger gap-closure planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification runner → test results | No new threat surface; verification only reads existing code and runs existing tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | Verification phase — test mutations | accept | Verification tests run against in-memory SQLite (conftest.py test_db fixture); production DB is never touched during pytest runs |
| T-04-11 | Elevation of Privilege | Human verification — admin data setup | accept | Human verifier uses existing admin account to create test data; this is expected admin workflow; no privilege escalation path |
</threat_model>

<verification>
All verification is performed within Task 1 (automated) and Task 2 (human checkpoint).

Automated gate (Task 1):
```bash
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/backend && python -m pytest tests/ -q
cd /Users/pvs/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit && npm run build
```

Human gate (Task 2): 17-item checklist covering PROF-01 through PROF-05.
</verification>

<success_criteria>
- Full pytest suite (tests/) exits 0
- TypeScript exits 0 (0 errors)
- npm run build succeeds
- Human verifier confirms all 17 checklist items in Task 2
- ROADMAP.md Phase 4 updated with plan list
- STATE.md updated with Phase 4 complete status
- Phase 4 summary created at .planning/phases/04-portal-do-professor/04-04-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-portal-do-professor/04-04-SUMMARY.md`
</output>
