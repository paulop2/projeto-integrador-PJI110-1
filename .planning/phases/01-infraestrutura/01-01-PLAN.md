---
phase: 01-infraestrutura
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main.py
  - backend/src/config.py
  - backend/src/database.py
  - backend/src/models/__init__.py
  - backend/requirements.txt
  - backend/pyproject.toml
  - backend/.env.example
  - .env.example
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Backend FastAPI responde com 200 em GET /health retornando {status: ok}"
    - "Variáveis de ambiente são lidas de backend/.env via pydantic-settings"
    - "SQLite engine configura WAL mode e foreign_keys=ON em toda nova conexão via event listener"
    - "CORS permite requisições de http://localhost:5173 para http://localhost:8000"
    - ".env nunca é commitado; .env.example documenta todas as variáveis necessárias"
  artifacts:
    - path: "backend/src/main.py"
      provides: "FastAPI app com CORSMiddleware e rota /health"
      contains: "CORSMiddleware"
    - path: "backend/src/config.py"
      provides: "Settings via pydantic-settings com BaseSettings"
      contains: "BaseSettings"
    - path: "backend/src/database.py"
      provides: "engine SQLAlchemy + event listener de pragmas SQLite + get_db dependency"
      contains: "event.listens_for"
    - path: "backend/requirements.txt"
      provides: "Dependências Python instaláveis"
      contains: "fastapi"
    - path: "backend/.env.example"
      provides: "Documentação de variáveis de ambiente obrigatórias"
      contains: "SECRET_KEY"
  key_links:
    - from: "backend/src/main.py"
      to: "backend/src/config.py"
      via: "settings.CORS_ORIGINS importado em main.py"
      pattern: "from src\\.config import settings"
    - from: "backend/src/database.py"
      to: "SQLite engine"
      via: "event listener PRAGMA por conexão"
      pattern: "event\\.listens_for.*connect"
    - from: "backend/src/config.py"
      to: "backend/.env"
      via: "pydantic-settings lê .env automaticamente"
      pattern: "SettingsConfigDict.*env_file"
---

<objective>
Criar o esqueleto completo do backend FastAPI: estrutura de pastas, configuração de dependências, carregamento de variáveis de ambiente, engine SQLite com pragmas corretos por conexão e app FastAPI com CORS configurado e endpoint /health.

Purpose: Fase 2+ depende deste backend funcionando. Todo o restante da Phase 1 (Alembic, frontend) pode avançar com este esqueleto como base.
Output: Backend executável via `uvicorn src.main:app --reload --port 8000` retornando 200 em /health.
</objective>

<execution_context>
@C:/Users/PVS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PVS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestrutura/01-CONTEXT.md
@.planning/phases/01-infraestrutura/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar estrutura de pastas e instalar dependências Python</name>
  <files>
    backend/requirements.txt
    backend/pyproject.toml
    backend/.env.example
    backend/src/__init__.py
    backend/src/models/__init__.py
    .gitignore
    .env.example
  </files>
  <action>
Criar a estrutura de pastas do backend e instalar todas as dependências Python.

**Estrutura a criar:**
```
backend/
  src/
    __init__.py          (vazio)
    models/
      __init__.py        (vazio por enquanto — será populado no plan 02)
  requirements.txt
  pyproject.toml
  .env.example
```

**backend/requirements.txt** — incluir exatamente estas dependências (Phase 1 + o que Phases 2+ precisam instalado agora per decisão de pesquisa):
```
fastapi[standard]>=0.115.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic-settings>=2.0.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.0.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
```

NÃO incluir `python-jose` — abandonado; usar PyJWT per pesquisa.
NÃO incluir `Black` em requirements.txt — apenas Ruff per decisão de discretion.

**backend/pyproject.toml** — configurar apenas Ruff (não Black separado, per decisão de discretion "use only Ruff"):
```toml
[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
```

**backend/.env.example:**
```bash
# backend/.env.example — copiar para .env e preencher valores reais
DATABASE_URL=sqlite:///./escola.db
SECRET_KEY=change-me-in-production-use-random-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=480
CORS_ORIGINS=["http://localhost:5173"]
ENVIRONMENT=development
```

**.env.example** na raiz do monorepo (aponta para o backend):
```bash
# .env.example — variáveis de ambiente do projeto
# Copiar backend/.env.example para backend/.env e preencher

# Backend (backend/.env)
# DATABASE_URL=sqlite:///./escola.db
# SECRET_KEY=change-me-in-production-use-random-32-chars
# ACCESS_TOKEN_EXPIRE_MINUTES=480
# CORS_ORIGINS=["http://localhost:5173"]
# ENVIRONMENT=development

# Frontend (frontend/.env.local)
# VITE_API_URL=http://localhost:8000
```

**.gitignore** na raiz (criar ou substituir se existir):
```gitignore
# Python
backend/venv/
backend/__pycache__/
backend/src/__pycache__/
backend/**/__pycache__/
backend/*.db
backend/*.db-shm
backend/*.db-wal
backend/.env

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env.local

# IDE
.vscode/
.idea/
*.pyc
*.pyo
```

**Criar venv e instalar dependências** (executar do diretório backend/):
```bash
cd backend
python -m venv venv
# Ativar no Git Bash:
source venv/Scripts/activate
pip install -r requirements.txt
```

Se `source venv/Scripts/activate` falhar (Windows CMD), usar: `venv\Scripts\activate.bat`
  </action>
  <verify>
```bash
cd backend
source venv/Scripts/activate
python -c "import fastapi, sqlalchemy, alembic, pydantic_settings, passlib, jwt; print('OK')"
```
Deve imprimir `OK` sem erros de importação.
  </verify>
  <done>Todas as dependências importam sem erro; requirements.txt, pyproject.toml e .env.example existem com conteúdo correto; .gitignore inclui venv/, *.db e .env.</done>
</task>

<task type="auto">
  <name>Task 2: Criar config.py, database.py e main.py com CORS e /health</name>
  <files>
    backend/src/config.py
    backend/src/database.py
    backend/src/main.py
    backend/.env
  </files>
  <action>
Criar os três arquivos core do backend e o arquivo .env de desenvolvimento.

**backend/src/config.py** — carregar .env via pydantic-settings com @lru_cache:
```python
from functools import lru_cache
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
    )

    DATABASE_URL: str = "sqlite:///./escola.db"
    SECRET_KEY: str = "dev-secret-key-change-in-production"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 480  # 8 horas
    CORS_ORIGINS: list[str] = ["http://localhost:5173"]
    ENVIRONMENT: str = "development"


@lru_cache
def get_settings() -> Settings:
    return Settings()


settings = get_settings()
```

**backend/src/database.py** — engine com event listener de pragmas SQLite por conexão:
```python
from sqlalchemy import create_engine, event, text
from sqlalchemy.orm import sessionmaker, DeclarativeBase

from src.config import settings


engine = create_engine(
    settings.DATABASE_URL,
    connect_args={"check_same_thread": False},  # Obrigatório para SQLite no FastAPI
)


@event.listens_for(engine, "connect")
def set_sqlite_pragmas(dbapi_conn, connection_record):
    """Configura pragmas SQLite em toda nova conexão — WAL mode + FK enforcement."""
    cursor = dbapi_conn.cursor()
    cursor.execute("PRAGMA journal_mode=WAL")   # WAL mode para leituras concorrentes
    cursor.execute("PRAGMA foreign_keys=ON")     # Enforça constraints de FK
    cursor.execute("PRAGMA busy_timeout=5000")   # Aguarda até 5s em write lock
    cursor.close()


SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


class Base(DeclarativeBase):
    pass


def get_db():
    """FastAPI dependency que fornece sessão de banco e fecha ao finalizar."""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

IMPORTANTE: NÃO chamar `Base.metadata.create_all(engine)` aqui — schema é gerenciado exclusivamente pelo Alembic (anti-pattern per pesquisa).

**backend/src/main.py** — app FastAPI com CORSMiddleware PRIMEIRO, rota /health:
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from src.config import settings


app = FastAPI(
    title="Sistema Escolar API",
    version="1.0.0",
    description="API do sistema de registro escolar",
)

# CORS deve ser adicionado PRIMEIRO — antes de qualquer outro middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,  # ["http://localhost:5173"] — nunca ["*"]
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/health")
def health_check():
    """Endpoint de verificação de saúde — confirma que API está online."""
    return {"status": "ok", "environment": settings.ENVIRONMENT}
```

NÃO usar `allow_origins=["*"]` com `allow_credentials=True` — browsers rejeitam esta combinação per pesquisa.

**backend/.env** — arquivo de desenvolvimento (git-ignored):
```bash
DATABASE_URL=sqlite:///./escola.db
SECRET_KEY=dev-secret-key-phase1-change-before-prod
ACCESS_TOKEN_EXPIRE_MINUTES=480
CORS_ORIGINS=["http://localhost:5173"]
ENVIRONMENT=development
```

Uvicorn deve ser executado a partir do diretório `backend/` para que `DATABASE_URL=sqlite:///./escola.db` resolva para `backend/escola.db`.
  </action>
  <verify>
```bash
cd backend
source venv/Scripts/activate
uvicorn src.main:app --port 8000 &
sleep 2
curl -s http://localhost:8000/health
# Deve retornar: {"status":"ok","environment":"development"}
curl -s -H "Origin: http://localhost:5173" -I http://localhost:8000/health | grep -i "access-control"
# Deve incluir: access-control-allow-origin: http://localhost:5173
kill %1
```
  </verify>
  <done>GET /health retorna 200 com {"status":"ok","environment":"development"}; header Access-Control-Allow-Origin inclui http://localhost:5173; uvicorn inicia sem erros de importação.</done>
</task>

</tasks>

<verification>
Verificações finais após ambas as tasks:

1. `python -c "import fastapi, sqlalchemy, alembic, pydantic_settings, passlib, jwt; print('OK')"` — imprime OK
2. `uvicorn src.main:app --port 8000` inicia sem erros
3. `curl http://localhost:8000/health` retorna `{"status":"ok","environment":"development"}`
4. `curl -H "Origin: http://localhost:5173" -I http://localhost:8000/health` inclui `Access-Control-Allow-Origin: http://localhost:5173`
5. `cat backend/.gitignore` confirma venv/, *.db, .env estão ignorados
6. `git status` confirma backend/.env NÃO aparece como arquivo rastreado
</verification>

<success_criteria>
- Backend FastAPI inicia em localhost:8000 sem erros
- GET /health retorna 200 com status "ok"
- CORS permite http://localhost:5173 (header presente na resposta)
- SQLite engine configurado com WAL + FK enforcement por evento de conexão
- .env existe localmente e NÃO é commitado; .env.example está commitado com todas as variáveis documentadas
- requirements.txt instala com `pip install -r requirements.txt` sem conflitos
</success_criteria>

<output>
After completion, create `.planning/phases/01-infraestrutura/01-01-SUMMARY.md`
</output>
