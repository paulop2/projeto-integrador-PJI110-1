---
phase: 01-infraestrutura
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/services/api.ts
  - frontend/.env.local
  - frontend/vite.config.ts
  - Makefile
autonomous: true

must_haves:
  truths:
    - "Frontend React inicia em localhost:5173 sem erros no console"
    - "App.tsx usa createBrowserRouter (não BrowserRouter legado) e ReactRouter DOM v6"
    - "main.tsx envolve o app com QueryClientProvider do TanStack Query v5"
    - "api.ts exporta instância axios com baseURL lida de import.meta.env.VITE_API_URL"
    - "`make backend` e `make frontend` iniciam os respectivos servidores em um comando"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "Entry point React com QueryClientProvider + RouterProvider"
      contains: "QueryClientProvider"
    - path: "frontend/src/App.tsx"
      provides: "Router configurado com createBrowserRouter"
      contains: "createBrowserRouter"
    - path: "frontend/src/services/api.ts"
      provides: "Instância axios configurada com baseURL do env"
      contains: "import.meta.env.VITE_API_URL"
    - path: "Makefile"
      provides: "Targets make backend / make frontend / make dev"
      contains: "uvicorn"
    - path: "frontend/.env.local"
      provides: "VITE_API_URL apontando para localhost:8000"
      contains: "VITE_API_URL"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "importa router exportado de App.tsx"
      pattern: "import.*router.*from.*App"
    - from: "frontend/src/services/api.ts"
      to: "http://localhost:8000"
      via: "baseURL=import.meta.env.VITE_API_URL"
      pattern: "VITE_API_URL"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/services/api.ts"
      via: "usa api.get('/health') para verificar CORS no componente raiz"
      pattern: "api\\.get.*health"
---

<objective>
Scaffoldar o frontend React 18 + TypeScript + Vite com React Router 6 (createBrowserRouter) e TanStack Query 5, configurar o cliente axios com baseURL via variável de ambiente e criar o Makefile de conveniência para o workflow de desenvolvimento.

Purpose: Frontend funcionando é pré-requisito para verificar CORS end-to-end. Sem este scaffold, os planos de backend não podem ser validados no contexto real.
Output: `npm run dev` serve em localhost:5173; `make dev` inicia backend + frontend em paralelo; App.tsx exibe resposta do backend /health confirmando CORS funcional.
</objective>

<execution_context>
@C:/Users/PVS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PVS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infraestrutura/01-CONTEXT.md
@.planning/phases/01-infraestrutura/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffoldar projeto Vite React-TS e instalar dependências</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/.env.local
  </files>
  <action>
Criar o projeto frontend com `npm create vite` e instalar as dependências necessárias.

**Executar a partir da raiz do monorepo:**
```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install react-router-dom @tanstack/react-query axios
npm install -D prettier
```

NÃO usar `npm create react-app` — depreciado. Usar `npm create vite@latest` per pesquisa.

**Após scaffold, verificar que frontend/package.json contém estas dependências:**
```json
{
  "dependencies": {
    "react": "^18.x",
    "react-dom": "^18.x",
    "react-router-dom": "^6.x",
    "@tanstack/react-query": "^5.x",
    "axios": "^1.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "vite": "^5.x",
    "prettier": "^3.x"
  }
}
```

**frontend/vite.config.ts** — manter o gerado pelo scaffold. NÃO configurar proxy Vite para o backend (usar CORS direto no FastAPI per pesquisa — o proxy esconderia problemas de CORS que aparecem em produção):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})
```

**frontend/.env.local** — criar arquivo (git-ignored automaticamente pelo Vite):
```bash
VITE_API_URL=http://localhost:8000
```

O arquivo `.env.local` é git-ignored pelo Vite por convenção. Confirmar que `frontend/.gitignore` (gerado pelo scaffold) contém `.env.local`.

**frontend/.prettierrc** — criar para formatação consistente:
```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```
  </action>
  <verify>
```bash
cd frontend
npm run build 2>&1 | tail -5
# Deve completar sem erros TypeScript
node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; ['react-router-dom','@tanstack/react-query','axios','prettier'].forEach(d => { if(!deps[d]) throw new Error('Missing: '+d); }); console.log('deps OK')"
```
  </verify>
  <done>npm run build completa sem erros; package.json tem react-router-dom, @tanstack/react-query, axios e prettier instalados; frontend/.env.local existe com VITE_API_URL=http://localhost:8000.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar main.tsx, App.tsx, api.ts e criar Makefile</name>
  <files>
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/services/api.ts
    Makefile
  </files>
  <action>
Configurar os arquivos core do frontend e o Makefile de desenvolvimento.

**frontend/src/services/api.ts** — criar diretório e arquivo:
```typescript
import axios from 'axios'

export const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
})
```

`import.meta.env.VITE_API_URL` resolve para o valor em `.env.local` durante `npm run dev`. Em produção, este env var deve ser definido no ambiente de build.

**frontend/src/App.tsx** — substituir o conteúdo gerado pelo scaffold:
```typescript
import { useEffect, useState } from 'react'
import { createBrowserRouter, RouterProvider, Link, Outlet } from 'react-router-dom'
import { api } from './services/api'

// Layout raiz — será expandido nas fases seguintes
function Root() {
  return (
    <div>
      <nav>
        <Link to="/">Início</Link>
      </nav>
      <Outlet />
    </div>
  )
}

// Página inicial com verificação de CORS
function HomePage() {
  const [backendStatus, setBackendStatus] = useState<string>('verificando...')

  useEffect(() => {
    api
      .get('/health')
      .then((res) => setBackendStatus(JSON.stringify(res.data)))
      .catch((err) => setBackendStatus(`Erro: ${err.message}`))
  }, [])

  return (
    <div>
      <h1>Sistema Escolar</h1>
      <p>Backend: {backendStatus}</p>
    </div>
  )
}

// Usar createBrowserRouter (data API) — NÃO usar BrowserRouter legado
export const router = createBrowserRouter([
  {
    path: '/',
    element: <Root />,
    children: [
      {
        index: true,
        element: <HomePage />,
      },
    ],
  },
])

export default function App() {
  return <RouterProvider router={router} />
}
```

**frontend/src/main.tsx** — substituir conteúdo gerado pelo scaffold:
```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { RouterProvider } from 'react-router-dom'
import { router } from './App'

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5, // 5 minutos
      retry: 1,
    },
  },
})

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={router} />
    </QueryClientProvider>
  </React.StrictMode>
)
```

NOTA: `RouterProvider` é importado tanto em `App.tsx` (export do router) quanto em `main.tsx` (render do RouterProvider). `App.tsx` exporta o objeto `router` para que `main.tsx` possa usá-lo.

**Makefile** — criar na raiz do monorepo:
```makefile
.PHONY: backend frontend dev install-backend install-frontend migrate setup

# Iniciar apenas o backend
backend:
	cd backend && source venv/Scripts/activate && uvicorn src.main:app --reload --port 8000

# Iniciar apenas o frontend
frontend:
	cd frontend && npm run dev

# Iniciar ambos em paralelo (requer make 3.81+)
dev:
	make -j2 backend frontend

# Instalar dependências do backend (criar venv se necessário)
install-backend:
	cd backend && python -m venv venv && source venv/Scripts/activate && pip install -r requirements.txt

# Instalar dependências do frontend
install-frontend:
	cd frontend && npm install

# Executar migrations Alembic
migrate:
	cd backend && source venv/Scripts/activate && alembic upgrade head

# Setup completo do projeto do zero
setup: install-backend install-frontend migrate
	@echo "Setup completo! Execute 'make dev' para iniciar."
```

NOTA sobre Windows: `source venv/Scripts/activate` funciona no Git Bash. Se usar CMD/PowerShell, executar manualmente:
- Backend: `cd backend && venv\Scripts\activate && uvicorn src.main:app --reload --port 8000`
- Frontend: `cd frontend && npm run dev`

Adicionar comentário no Makefile explicando compatibilidade com Git Bash.
  </action>
  <verify>
```bash
# 1. Verificar build TypeScript não tem erros
cd frontend && npm run build 2>&1 | grep -E "error|Error" | head -5
# Não deve ter saída de erro

# 2. Verificar que main.tsx contém QueryClientProvider
grep -l "QueryClientProvider" frontend/src/main.tsx && echo "QueryClientProvider OK"

# 3. Verificar que App.tsx usa createBrowserRouter
grep -l "createBrowserRouter" frontend/src/App.tsx && echo "createBrowserRouter OK"

# 4. Verificar que api.ts usa VITE_API_URL
grep -l "VITE_API_URL" frontend/src/services/api.ts && echo "VITE_API_URL OK"

# 5. Verificar Makefile existe e tem targets esperados
grep -E "^backend:|^frontend:|^dev:|^migrate:" Makefile && echo "Makefile targets OK"
```

Para verificação end-to-end de CORS (requer backend rodando do plan 01):
```bash
# Terminal 1: iniciar backend
cd backend && source venv/Scripts/activate && uvicorn src.main:app --reload --port 8000

# Terminal 2: verificar CORS com curl simulando origin do frontend
curl -s -H "Origin: http://localhost:5173" http://localhost:8000/health
# Deve retornar: {"status":"ok","environment":"development"}
# e o header Access-Control-Allow-Origin deve estar presente
```
  </verify>
  <done>npm run build completa sem erros TypeScript; main.tsx tem QueryClientProvider; App.tsx usa createBrowserRouter; api.ts usa import.meta.env.VITE_API_URL; Makefile tem targets backend, frontend, dev, migrate e setup na raiz do monorepo.</done>
</task>

</tasks>

<verification>
Verificações finais deste plano:

1. `cd frontend && npm run dev` inicia sem erros em localhost:5173
2. Browser em localhost:5173 exibe "Backend: {"status":"ok","environment":"development"}" (confirma CORS funcional com backend rodando)
3. DevTools Network mostra requisição para localhost:8000/health com status 200 e header Access-Control-Allow-Origin presente
4. `cd frontend && npm run build` completa sem erros TypeScript
5. `make backend` inicia uvicorn (Git Bash)
6. `make frontend` inicia vite dev server (Git Bash)
</verification>

<success_criteria>
- `npm run dev` serve em localhost:5173 sem erros
- `npm run build` compila TypeScript sem erros
- App.tsx exibe status do backend via chamada /health confirmando CORS end-to-end
- main.tsx usa QueryClientProvider (base para TanStack Query nas fases seguintes)
- App.tsx usa createBrowserRouter (data API, não BrowserRouter legado)
- Makefile na raiz com targets backend, frontend, dev, migrate, setup
- frontend/.env.local tem VITE_API_URL=http://localhost:8000 e está git-ignored
</success_criteria>

<output>
After completion, create `.planning/phases/01-infraestrutura/01-03-SUMMARY.md`
</output>
