---
plan: "08-01"
phase: 8
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/Sidebar.tsx
  - frontend/src/components/admin/AdminLayout.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Sidebar do admin possui botão hamburger no topo que colapsa a sidebar para ícones de 56px no desktop"
    - "Em viewport < 768px a sidebar está oculta por padrão e um hamburger no header abre overlay drawer"
    - "Backdrop semitransparente aparece sobre o conteúdo quando o drawer mobile está aberto e fecha ao clicar"
    - "AdminLayout tem header fixo (h-16) com logo à esquerda e área para UserMenu à direita"
    - "No estado colapsado só ícones SVG são visíveis com tooltip ao hover; labels e seção 'Menu' somem"
  artifacts:
    - path: "frontend/src/components/admin/Sidebar.tsx"
      provides: "Sidebar com props collapsed e onToggle, hamburger button, estados expanded/collapsed, tooltip nos ícones"
      contains: "collapsed: boolean"
    - path: "frontend/src/components/admin/AdminLayout.tsx"
      provides: "Layout com useState para sidebarCollapsed e mobileOpen, header h-16, mobile drawer overlay"
      contains: "sidebarCollapsed"
  key_links:
    - from: "AdminLayout.tsx"
      to: "Sidebar.tsx"
      via: "collapsed={sidebarCollapsed} onToggle={() => setSidebarCollapsed(prev => !prev)}"
      pattern: "collapsed=\\{sidebarCollapsed\\}"
    - from: "AdminLayout header"
      to: "mobile drawer"
      via: "onClick={() => setMobileOpen(true)} md:hidden"
      pattern: "setMobileOpen"
---

<objective>
Adicionar sidebar colapsável com hamburger button ao painel admin e reestruturar AdminLayout com header fixo e suporte a overlay mobile.

Purpose: Success criteria 1 e 2 da Phase 8 — sidebar com collapse no desktop e layout mobile sem transbordamento horizontal.
Output: Sidebar.tsx com props collapsed/onToggle + hamburger + tooltips; AdminLayout.tsx com estado, header h-16, mobile drawer overlay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ux-polish/08-CONTEXT.md
@.planning/phases/08-ux-polish/08-UI-SPEC.md

<interfaces>
<!-- Interfaces e exports extraídos dos arquivos atuais — o executor NÃO precisa explorar o codebase -->

From frontend/src/components/admin/Sidebar.tsx (estado atual):
```tsx
// Sem props — estado atual não recebe collapsed/onToggle
export default function Sidebar() { ... }
// Usa: useAuth(), useNavigate()
// Tem: navItems array, IconDashboard/Users/School/Book/Teacher/Family/Logout inline SVGs
// Footer user: initials = user.nome.split(' ').slice(0,2).map(n=>n[0]).join('').toUpperCase()
// Classes atuais: aside "w-60 bg-gray-950 text-white flex flex-col flex-shrink-0 border-r border-gray-800"
```

From frontend/src/components/admin/AdminLayout.tsx (estado atual):
```tsx
export default function AdminLayout() {
  return (
    <div className="flex h-screen overflow-hidden bg-gray-50">
      <Sidebar />
      <main className="flex-1 overflow-y-auto">
        <Outlet />
      </main>
    </div>
  )
}
```

From frontend/src/contexts/AuthContext.tsx (padrão de uso):
```ts
const { user, logout } = useAuth()
// user.nome: string, user.email: string, user.tipo: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar Sidebar com props collapsed/onToggle, hamburger e tooltips</name>
  <files>frontend/src/components/admin/Sidebar.tsx</files>
  <read_first>
    - frontend/src/components/admin/Sidebar.tsx (leitura obrigatória — ver estado atual completo antes de qualquer edição)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-01 (contratos exatos de JSX, classes Tailwind e ARIA)
  </read_first>
  <action>
Reescrever Sidebar.tsx para aceitar props `collapsed: boolean` e `onToggle: () => void`. Manter todos os imports existentes e adicionar `NavLink` se não estiver importado. A assinatura da função muda de `export default function Sidebar()` para `export default function Sidebar({ collapsed, onToggle }: { collapsed: boolean; onToggle: () => void })`.

SEÇÃO LOGO (substituir bloco logo atual — px-5 py-5):

Estado expanded:
```tsx
<div className="px-5 py-4 border-b border-gray-800 flex items-center justify-between">
  <div className="flex items-center gap-3">
    <div className="w-8 h-8 rounded-lg bg-indigo-600 flex items-center justify-center flex-shrink-0">
      {/* SVG do chapéu de graduação existente — manter */}
    </div>
    <div>
      <p className="text-sm font-semibold text-white leading-tight">EscolaApp</p>
      <p className="text-xs text-gray-500 leading-tight">Painel Administrativo</p>
    </div>
  </div>
  <button
    onClick={onToggle}
    className="p-2 rounded-md text-gray-400 hover:bg-gray-800 hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500"
    aria-label="Colapsar menu"
    aria-expanded={true}
    aria-controls="admin-sidebar-nav"
  >
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <line x1="3" y1="6" x2="21" y2="6" />
      <line x1="3" y1="12" x2="21" y2="12" />
      <line x1="3" y1="18" x2="21" y2="18" />
    </svg>
  </button>
</div>
```

Estado collapsed (renderizado quando `collapsed === true`):
```tsx
<div className="px-0 py-4 border-b border-gray-800 flex items-center justify-center">
  <button
    onClick={onToggle}
    className="p-2 rounded-md text-gray-400 hover:bg-gray-800 hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500"
    aria-label="Expandir menu"
    aria-expanded={false}
    aria-controls="admin-sidebar-nav"
  >
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <line x1="3" y1="6" x2="21" y2="6" />
      <line x1="3" y1="12" x2="21" y2="12" />
      <line x1="3" y1="18" x2="21" y2="18" />
    </svg>
  </button>
</div>
```

Implementar com renderização condicional: `{collapsed ? <CollapsedLogoSection /> : <ExpandedLogoSection />}` ou inline com ternários.

ELEMENTO ASIDE (substituir a tag aside atual):
```tsx
<aside
  aria-label="Menu de navegação administrativo"
  className={`flex-shrink-0 bg-gray-950 text-white flex flex-col border-r border-gray-800 transition-all duration-300 ease-in-out ${collapsed ? 'w-14' : 'w-60'}`}
>
```

SEÇÃO NAV — seção "Menu" heading e labels:
- Heading "Menu": `{!collapsed && <p className="px-2 pb-2 text-[10px] font-semibold text-gray-500 uppercase tracking-widest">Menu</p>}`
- Adicionar `id="admin-sidebar-nav"` na tag `<nav>`
- NavItems com label condicional e tooltip:

```tsx
{navItems.map(({ to, label, end, Icon }) => (
  <NavLink
    key={to}
    to={to}
    end={end}
    className={({ isActive }) =>
      collapsed
        ? `flex items-center justify-center px-0 py-3 rounded-md transition-colors ${isActive ? 'bg-indigo-600 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white'}`
        : `flex items-center gap-3 px-3 py-2 rounded-md text-sm font-semibold transition-colors ${isActive ? 'bg-indigo-600 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white'}`
    }
  >
    {collapsed ? (
      <span className="relative group">
        <Icon />
        <span className="absolute left-full ml-2 top-1/2 -translate-y-1/2 px-2 py-1 bg-gray-900 text-white text-xs rounded whitespace-nowrap opacity-0 group-hover:opacity-100 transition-opacity pointer-events-none z-50">
          {label}
        </span>
      </span>
    ) : (
      <>
        <Icon />
        <span>{label}</span>
      </>
    )}
  </NavLink>
))}
```

FOOTER USER SECTION (D-07 — preservar mas adaptar para collapsed):
```tsx
<div className="p-3 border-t border-gray-800">
  {collapsed ? (
    <div className="flex justify-center p-2">
      <div className="w-7 h-7 rounded-full bg-indigo-500/20 text-indigo-400 flex items-center justify-center text-xs font-semibold">
        {initials}
      </div>
    </div>
  ) : (
    <div className="flex items-center gap-3 px-2 py-2 rounded-md hover:bg-gray-800 transition-colors group">
      <div className="w-7 h-7 rounded-full bg-indigo-500/20 text-indigo-400 flex items-center justify-center text-xs font-bold flex-shrink-0">
        {initials}
      </div>
      <div className="flex-1 min-w-0">
        <p className="text-xs text-white font-medium truncate">{user?.nome}</p>
        <p className="text-[10px] text-gray-500 truncate">{user?.email}</p>
      </div>
      <button
        onClick={handleLogout}
        className="opacity-0 group-hover:opacity-100 text-gray-500 hover:text-white transition-all"
        title="Sair"
      >
        <IconLogout />
      </button>
    </div>
  )}
</div>
```

Manter `handleLogout`, `initials`, `useAuth()`, `useNavigate()` e todos os Icon components existentes.
  </action>
  <verify>
    <automated>cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/admin/Sidebar.tsx contém `collapsed: boolean` na assinatura da função
    - frontend/src/components/admin/Sidebar.tsx contém `onToggle: () => void`
    - frontend/src/components/admin/Sidebar.tsx contém `transition-all duration-300 ease-in-out`
    - frontend/src/components/admin/Sidebar.tsx contém `w-14` (collapsed width)
    - frontend/src/components/admin/Sidebar.tsx contém `w-60` (expanded width)
    - frontend/src/components/admin/Sidebar.tsx contém `aria-label="Menu de navegação administrativo"`
    - frontend/src/components/admin/Sidebar.tsx contém `id="admin-sidebar-nav"`
    - frontend/src/components/admin/Sidebar.tsx contém `aria-label="Colapsar menu"`
    - frontend/src/components/admin/Sidebar.tsx contém `aria-label="Expandir menu"`
    - frontend/src/components/admin/Sidebar.tsx contém `group-hover:opacity-100` (tooltip visibility)
    - frontend/src/components/admin/Sidebar.tsx contém `left-full ml-2` (tooltip position)
    - TypeScript build sem erros relacionados a Sidebar.tsx
  </acceptance_criteria>
  <done>Sidebar.tsx aceita collapsed/onToggle, anima width com transition-all, hamburger button com ARIA correto, nav items com tooltip no estado collapsed, footer user section adaptado para ambos os estados, TypeScript compilando sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar AdminLayout com estado collapsed/mobile, header h-16 e mobile overlay drawer</name>
  <files>frontend/src/components/admin/AdminLayout.tsx</files>
  <read_first>
    - frontend/src/components/admin/AdminLayout.tsx (leitura obrigatória — ver estado atual antes de editar)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-02 (JSX completo do AdminLayout, estados, breakpoints)
    - frontend/src/components/AppLayout.tsx (referência para estilo do header — bg-white shadow-sm border-b border-gray-200)
  </read_first>
  <action>
Reescrever AdminLayout.tsx adicionando imports `useState` do React e `UserMenu` (que será criado em 08-02). O arquivo vai de 13 linhas para ~60 linhas.

Imports:
```tsx
import { useState } from 'react'
import { Outlet } from 'react-router-dom'
import Sidebar from './Sidebar'
import { UserMenu } from '../UserMenu'
```

Estado:
```tsx
const [sidebarCollapsed, setSidebarCollapsed] = useState(false)
const [mobileOpen, setMobileOpen] = useState(false)
```

Estrutura JSX completa:
```tsx
return (
  <div className="flex h-screen overflow-hidden bg-gray-50">
    {/* Desktop sidebar — oculta abaixo do breakpoint md (768px) */}
    <div className="hidden md:flex">
      <Sidebar
        collapsed={sidebarCollapsed}
        onToggle={() => setSidebarCollapsed((prev) => !prev)}
      />
    </div>

    {/* Mobile overlay drawer — visível apenas quando mobileOpen === true */}
    {mobileOpen && (
      <>
        {/* Backdrop semitransparente — cobre o conteúdo principal */}
        <div
          className="fixed inset-0 bg-black/50 z-40 md:hidden"
          onClick={() => setMobileOpen(false)}
          aria-hidden="true"
        />
        {/* Drawer panel — sidebar completa não colapsada */}
        <div
          id="admin-mobile-drawer"
          className="fixed inset-y-0 left-0 z-50 md:hidden"
        >
          <Sidebar
            collapsed={false}
            onToggle={() => setMobileOpen(false)}
          />
        </div>
      </>
    )}

    {/* Coluna direita: header + main */}
    <div className="flex flex-col flex-1 min-w-0 overflow-hidden">
      {/* Header fixo h-16 */}
      <header className="h-16 bg-white border-b border-gray-200 shadow-sm flex items-center justify-between px-4 flex-shrink-0 z-30">
        {/* Hamburger para mobile — oculto no desktop (md+) */}
        <button
          onClick={() => setMobileOpen(true)}
          className="md:hidden p-2 rounded-md text-gray-500 hover:bg-gray-100 hover:text-gray-700 transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500"
          aria-label="Abrir menu"
          aria-expanded={mobileOpen}
          aria-controls="admin-mobile-drawer"
        >
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
            <line x1="3" y1="6" x2="21" y2="6" />
            <line x1="3" y1="12" x2="21" y2="12" />
            <line x1="3" y1="18" x2="21" y2="18" />
          </svg>
        </button>

        {/* Logo — visível apenas no mobile (desktop tem logo na sidebar) */}
        <span className="md:hidden text-xl font-semibold text-indigo-600">EscolaApp</span>
        {/* Spacer no desktop */}
        <span className="hidden md:block" />

        {/* UserMenu — avatar + dropdown */}
        <UserMenu />
      </header>

      {/* Main content area */}
      <main id="conteudo-principal" className="flex-1 overflow-y-auto">
        <Outlet />
      </main>
    </div>
  </div>
)
```

NOTA: O import de `UserMenu` de `'../UserMenu'` será resolvido quando 08-02 criar o arquivo. Se o TypeScript reclamar durante execução de 08-01 antes de 08-02, crie um stub temporário: `export function UserMenu() { return null }` em `frontend/src/components/UserMenu.tsx`, que será substituído por 08-02.
  </action>
  <verify>
    <automated>cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/admin/AdminLayout.tsx contém `sidebarCollapsed`
    - frontend/src/components/admin/AdminLayout.tsx contém `mobileOpen`
    - frontend/src/components/admin/AdminLayout.tsx contém `hidden md:flex` (desktop sidebar wrapper)
    - frontend/src/components/admin/AdminLayout.tsx contém `bg-black/50 z-40 md:hidden` (backdrop)
    - frontend/src/components/admin/AdminLayout.tsx contém `id="admin-mobile-drawer"`
    - frontend/src/components/admin/AdminLayout.tsx contém `h-16 bg-white border-b border-gray-200 shadow-sm` (header)
    - frontend/src/components/admin/AdminLayout.tsx contém `aria-label="Abrir menu"`
    - frontend/src/components/admin/AdminLayout.tsx contém `md:hidden` no hamburger do header
    - frontend/src/components/admin/AdminLayout.tsx contém `<UserMenu />`
    - frontend/src/components/admin/AdminLayout.tsx contém `id="conteudo-principal"` no main
    - TypeScript build sem erros de tipo em AdminLayout.tsx
  </acceptance_criteria>
  <done>AdminLayout.tsx gerencia estado collapsed e mobile, desktop sidebar usa hidden md:flex, mobile drawer overlay com backdrop, header fixo h-16 com hamburger mobile e UserMenu, main com overflow-y-auto, TypeScript compilando sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI state | sidebarCollapsed e mobileOpen são estado local do browser — não cruzam rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | AdminLayout mobile drawer | accept | Drawer usa mesmos navItems que a sidebar desktop — sem informação adicional exposta; proteção de rotas já existe via ProtectedRoute |
| T-08-02 | Denial of Service | z-index stacking (z-40 backdrop, z-50 drawer) | accept | Valores de z-index são conservadores e não conflitam com toasts (sonner usa z-[9999]) |
</threat_model>

<verification>
Após execução das duas tasks:

```bash
cd frontend && npx tsc --noEmit
```

Deve sair com código 0 (sem erros).

Verificação manual mínima:
1. Abrir http://localhost:5173/admin no desktop → sidebar visível em w-60
2. Clicar hamburger na sidebar → sidebar colapsa para w-14, apenas ícones visíveis
3. Hover sobre ícone colapsado → tooltip com label aparece
4. Reduzir viewport < 768px → sidebar some, hamburger aparece no header
5. Clicar hamburger no header → drawer overlay aparece com backdrop
6. Clicar no backdrop → drawer fecha
</verification>

<success_criteria>
- Sidebar.tsx aceita `collapsed` e `onToggle` como props sem erro de TypeScript
- Transição `transition-all duration-300 ease-in-out` anima a largura entre w-14 e w-60
- AdminLayout.tsx tem header fixo h-16 com hamburger mobile e UserMenu
- Mobile overlay drawer renderiza condicional com backdrop bg-black/50
- `npm run build` no frontend passa sem erros de TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-ux-polish/08-01-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
