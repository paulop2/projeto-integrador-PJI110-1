---
plan: "08-03"
phase: 8
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - frontend/src/components/admin/EntityTable.tsx
  - frontend/src/components/responsavel/BoletimTable.tsx
  - frontend/src/components/professor/GradeTable.tsx
  - frontend/src/pages/professor/ProfessorTurmaPage.tsx
  - frontend/src/pages/responsavel/ResponsavelBoletimPage.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "EntityTable, BoletimTable e GradeTable são legíveis no mobile com scroll horizontal — overflow-x-auto consistente em todos os estados (loading, vazio, com dados)"
    - "ProfessorTurmaPage usa padding responsivo px-4 sm:px-6 lg:px-8 em vez de p-8 fixo"
    - "Nenhuma regressão funcional — TypeScript build passa, todas as features existentes operam"
  artifacts:
    - path: "frontend/src/components/admin/EntityTable.tsx"
      provides: "Tabela admin com overflow-x-auto no estado de loading (SkeletonTable) e no estado com dados"
      contains: "overflow-x-auto"
    - path: "frontend/src/pages/professor/ProfessorTurmaPage.tsx"
      provides: "Outer div com padding responsivo px-4 sm:px-6 lg:px-8"
      contains: "px-4 sm:px-6 lg:px-8"
  key_links:
    - from: "EntityTable.tsx isLoading branch"
      to: "SkeletonTable"
      via: "div className overflow-x-auto wrapping SkeletonTable"
      pattern: "overflow-x-auto"
    - from: "ProfessorTurmaPage outer div"
      to: "mobile viewport"
      via: "px-4 sm:px-6 lg:px-8 py-6"
      pattern: "px-4 sm:px-6 lg:px-8"
---

<objective>
Garantir responsividade das tabelas de dados e padding das páginas de professor e responsável, seguida de verificação completa do TypeScript build para confirmar zero regressões.

Purpose: Success criteria 4 e 5 da Phase 8 — tabelas legíveis no mobile e build limpo confirmando que nenhuma feature quebrou.
Output: EntityTable com overflow-x-auto no estado loading; ProfessorTurmaPage com padding responsivo; confirmação de build limpo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ux-polish/08-CONTEXT.md
@.planning/phases/08-ux-polish/08-UI-SPEC.md
@.planning/phases/08-ux-polish/08-01-SUMMARY.md
@.planning/phases/08-ux-polish/08-02-SUMMARY.md

<interfaces>
<!-- Estado atual relevante dos arquivos — extraído antes do planejamento -->

From frontend/src/components/admin/EntityTable.tsx (estado atual — problema a corrigir):
```tsx
// Linha 61-65: loading branch SEM overflow-x-auto (bug a corrigir)
{isLoading ? (
  <SkeletonTable rows={5} columns={columns.length + 1} />  // ← SEM wrapper overflow-x-auto
) : rows.length === 0 ? (
  <div className="text-center py-12 text-gray-400 text-sm">Nenhum registro encontrado.</div>
) : (
  <div className="overflow-x-auto">  // ← overflow-x-auto SÓ existe aqui (dados presentes)
    <table className="min-w-full divide-y divide-gray-200">
```

From frontend/src/components/responsavel/BoletimTable.tsx (estado atual — JÁ COMPLIANT):
```tsx
// Linha 37-39: overflow-x-auto JÁ presente — apenas verificar
return (
  <div>
    <div className="overflow-x-auto">
      <table className="min-w-[640px] min-w-full divide-y divide-gray-200">
```

From frontend/src/components/professor/GradeTable.tsx (estado atual — JÁ COMPLIANT):
```tsx
// Linha 97-99: overflow-x-auto JÁ presente — apenas verificar
return (
  <div>
    <div className="overflow-x-auto">
      <table className="min-w-full divide-y divide-gray-200">
```

From frontend/src/pages/professor/ProfessorTurmaPage.tsx (problema a corrigir):
// Linha 182: outer div com p-8 fixo (32px todos os lados — quebra mobile)
<div className="p-8">

From frontend/src/pages/responsavel/ResponsavelBoletimPage.tsx (JÁ COMPLIANT):
// Linha 62-63: pt-16 outer + max-w-7xl com px-4 sm:px-6 lg:px-8 inner — OK
<div className="pt-16">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir EntityTable (overflow-x-auto no loading state) e ProfessorTurmaPage (padding responsivo)</name>
  <files>
    frontend/src/components/admin/EntityTable.tsx
    frontend/src/pages/professor/ProfessorTurmaPage.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/EntityTable.tsx (leitura obrigatória — ver estado atual inteiro, focar nas linhas 60-70 do branch isLoading)
    - frontend/src/pages/professor/ProfessorTurmaPage.tsx (leitura obrigatória — encontrar linha 182 com "p-8" e confirmar contexto ao redor)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-04 (instruções para EntityTable) e §C-05 (instrução para ProfessorTurmaPage)
  </read_first>
  <action>
MUDANÇA 1 — EntityTable.tsx:

Localizar o branch `isLoading` (em torno da linha 61). Atualmente:
```tsx
{isLoading ? (
  <SkeletonTable rows={5} columns={columns.length + 1} />
```

Envolver SkeletonTable em div com overflow-x-auto (espelhar o wrapper do branch de dados):
```tsx
{isLoading ? (
  <div className="overflow-x-auto">
    <SkeletonTable rows={5} columns={columns.length + 1} />
  </div>
```

O branch de dados já tem `<div className="overflow-x-auto">` — NÃO alterar.
O branch vazio (`rows.length === 0`) não contém tabela — NÃO precisa de overflow-x-auto.
Nenhuma outra linha de EntityTable.tsx deve ser alterada.

MUDANÇA 2 — ProfessorTurmaPage.tsx:

Localizar a linha com `<div className="p-8">` (em torno da linha 182 — é o outer wrapper da página, logo após o return).

Substituir:
```tsx
<div className="p-8">
```

Por:
```tsx
<div className="px-4 sm:px-6 lg:px-8 py-6">
```

Nenhuma outra linha de ProfessorTurmaPage.tsx deve ser alterada. Preservar TODA a lógica interna de chamada, notas, frequência, tabs, modais, etc.
  </action>
  <verify>
    <automated>grep -n "overflow-x-auto" /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend/src/components/admin/EntityTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EntityTable.tsx: `grep -c "overflow-x-auto"` retorna 2 (um no loading branch, um no dados branch)
    - EntityTable.tsx contém `<div className="overflow-x-auto">` imediatamente antes de `<SkeletonTable`
    - ProfessorTurmaPage.tsx NÃO contém `className="p-8"` no outer wrapper
    - ProfessorTurmaPage.tsx contém `px-4 sm:px-6 lg:px-8 py-6`
    - ProfessorTurmaPage.tsx ainda contém `TabNav` (preservado — sem regressão de tabs)
    - ProfessorTurmaPage.tsx ainda contém `AttendanceToggle` (preservado — sem regressão de chamada)
    - ProfessorTurmaPage.tsx ainda contém `GradeTable` (preservado — sem regressão de notas)
  </acceptance_criteria>
  <done>EntityTable tem overflow-x-auto no estado loading além do estado com dados; ProfessorTurmaPage usa padding responsivo px-4 sm:px-6 lg:px-8 py-6; toda a lógica funcional das páginas preservada sem alterações.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar BoletimTable, GradeTable e ResponsavelBoletimPage + TypeScript build completo</name>
  <files>
    frontend/src/components/responsavel/BoletimTable.tsx
    frontend/src/components/professor/GradeTable.tsx
    frontend/src/pages/responsavel/ResponsavelBoletimPage.tsx
  </files>
  <read_first>
    - frontend/src/components/responsavel/BoletimTable.tsx (verificar overflow-x-auto e min-w-[640px])
    - frontend/src/components/professor/GradeTable.tsx (verificar overflow-x-auto)
    - frontend/src/pages/responsavel/ResponsavelBoletimPage.tsx (verificar px-4 sm:px-6 lg:px-8)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-04 e §C-05 (confirmar o que é "already compliant" vs o que precisa de mudança)
  </read_first>
  <action>
VERIFICAÇÃO 1 — BoletimTable.tsx:

Ler o arquivo inteiro. Confirmar que o retorno do componente tem estrutura:
```tsx
return (
  <div>
    <div className="overflow-x-auto">
      <table className="min-w-[640px] ...">
```

Se `overflow-x-auto` já está presente: nenhuma mudança necessária. Se ausente (improvável, mas verificar): adicionar `<div className="overflow-x-auto">` envolvendo a `<table>` e o fechamento correspondente `</div>`.

VERIFICAÇÃO 2 — GradeTable.tsx:

Ler o arquivo inteiro. Confirmar que o return (em torno da linha 97) contém:
```tsx
<div>
  <div className="overflow-x-auto">
    <table className="min-w-full ...">
```

Se presente: nenhuma mudança necessária. Se ausente: adicionar wrapper `<div className="overflow-x-auto">`.

VERIFICAÇÃO 3 — ResponsavelBoletimPage.tsx:

Confirmar que o outer div tem `pt-16` e o inner container tem `px-4 sm:px-6 lg:px-8`. Se presente: nenhuma mudança necessária.

VERIFICAÇÃO FINAL — TypeScript build:

Após todas as verificações e eventuais correções, executar o build completo:
```bash
cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npm run build
```

Se o build falhar por qualquer erro de TypeScript introduzido em 08-01 ou 08-02 (ex: UserMenu import não resolvido, Sidebar props incompatíveis), diagnosticar e corrigir antes de marcar o plano como concluído. O critério de aceite desta task é build 100% limpo.
  </action>
  <verify>
    <automated>cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/responsavel/BoletimTable.tsx contém `overflow-x-auto`
    - frontend/src/components/professor/GradeTable.tsx contém `overflow-x-auto`
    - frontend/src/pages/responsavel/ResponsavelBoletimPage.tsx contém `px-4 sm:px-6 lg:px-8`
    - `npm run build` no frontend/  retorna exit code 0
    - Output do build contém `✓` ou `built in` sem linhas de erro TypeScript
    - Output do build NÃO contém `error TS` em nenhuma linha
    - Output do build NÃO contém `Cannot find module` em nenhuma linha
  </acceptance_criteria>
  <done>BoletimTable, GradeTable e ResponsavelBoletimPage verificadas como compliant com D-09/D-11; TypeScript build do frontend passa com exit code 0 sem erros — confirmando zero regressões em todas as features existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client rendering | Mudanças são puramente de CSS/layout — não cruzam boundaries de rede ou autenticação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | EntityTable overflow wrapper | accept | Adicionar div wrapper de CSS não muda lógica de dados, validação ou permissões; risco zero |
| T-08-06 | Denial of Service | npm run build failure | mitigate | Task 2 executa build completo e exige exit code 0 antes de concluir — qualquer regressão de TypeScript é detectada e corrigida neste plano |
</threat_model>

<verification>
Verificações finais de todo o Phase 8:

```bash
# 1. TypeScript build limpo
cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npm run build

# 2. overflow-x-auto em EntityTable (deve retornar 2 ocorrências)
grep -c "overflow-x-auto" frontend/src/components/admin/EntityTable.tsx

# 3. Padding responsivo em ProfessorTurmaPage
grep "px-4 sm:px-6 lg:px-8" frontend/src/pages/professor/ProfessorTurmaPage.tsx

# 4. UserMenu importado no AppLayout
grep "UserMenu" frontend/src/components/AppLayout.tsx

# 5. Sidebar aceita collapsed prop
grep "collapsed: boolean" frontend/src/components/admin/Sidebar.tsx

# 6. AdminLayout tem header e mobile overlay
grep "sidebarCollapsed" frontend/src/components/admin/AdminLayout.tsx
grep "mobileOpen" frontend/src/components/admin/AdminLayout.tsx
```

Todos devem retornar correspondências (exit code 0) exceto o build que deve ter exit code 0.
</verification>

<success_criteria>
- `npm run build` no diretório frontend/ passa com exit code 0
- EntityTable tem overflow-x-auto em 2 lugares (loading e dados presentes)
- ProfessorTurmaPage usa `px-4 sm:px-6 lg:px-8 py-6` no outer wrapper
- BoletimTable e GradeTable confirmadas com overflow-x-auto existente
- Nenhuma linha de erro `TS` no output do build
- Todos os 5 success criteria da Phase 8 cobertos pelas 3 plans executadas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-ux-polish/08-03-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
