---
plan: "08-02"
phase: 8
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/UserMenu.tsx
  - frontend/src/components/AppLayout.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Todos os perfis (admin, professor, responsável) têm avatar com iniciais e dropdown com nome, tipo e botão Sair"
    - "Componente UserMenu é reutilizável — usado em AppLayout e AdminLayout sem duplicação de lógica"
    - "Dropdown fecha ao clicar fora e ao pressionar Escape"
    - "AppLayout usa UserMenu no lugar do botão textual atual"
  artifacts:
    - path: "frontend/src/components/UserMenu.tsx"
      provides: "Componente reutilizável com avatar círculo indigo, dropdown com nome/tipo/Sair"
      exports: ["UserMenu"]
      contains: "useAuth"
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "Layout de professor/responsável usando UserMenu em vez de botão textual"
      contains: "UserMenu"
  key_links:
    - from: "UserMenu.tsx"
      to: "useAuth()"
      via: "const { user, logout } = useAuth()"
      pattern: "useAuth"
    - from: "AppLayout.tsx"
      to: "UserMenu.tsx"
      via: "import { UserMenu } from './UserMenu'"
      pattern: "import.*UserMenu"
---

<objective>
Criar componente UserMenu reutilizável com avatar + dropdown e atualizar AppLayout para usá-lo, eliminando a duplicação de lógica de dropdown entre AppLayout e AdminLayout.

Purpose: Success criteria 3 da Phase 8 — todos os perfis têm avatar com iniciais e dropdown padronizado com nome, tipo e logout.
Output: UserMenu.tsx (novo componente) + AppLayout.tsx (substituição do botão textual por UserMenu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ux-polish/08-CONTEXT.md
@.planning/phases/08-ux-polish/08-UI-SPEC.md

<interfaces>
<!-- Interfaces e lógica extraídas dos arquivos existentes — executor NÃO precisa explorar o codebase -->

From frontend/src/components/AppLayout.tsx (lógica de dropdown atual — migrar para UserMenu):
```tsx
// Imports relevantes
import { useState, useEffect, useRef, useMemo } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../contexts/AuthContext'

// tipoLabelMap — copiar para UserMenu
const tipoLabelMap: Record<string, string> = {
  admin: 'Admin',
  professor: 'Professor',
  responsavel: 'Responsável',
}

// Estado e refs — migrar para UserMenu
const [dropdownOpen, setDropdownOpen] = useState(false)
const dropdownRef = useRef<HTMLDivElement>(null)

// Click outside handler — migrar para UserMenu
useEffect(() => {
  function handleClickOutside(event: MouseEvent) {
    if (dropdownRef.current && !dropdownRef.current.contains(event.target as Node)) {
      setDropdownOpen(false)
    }
  }
  if (dropdownOpen) { document.addEventListener('mousedown', handleClickOutside) }
  return () => { document.removeEventListener('mousedown', handleClickOutside) }
}, [dropdownOpen])

// Escape handler — migrar para UserMenu
useEffect(() => {
  function handleEscape(event: KeyboardEvent) {
    if (event.key === 'Escape') { setDropdownOpen(false) }
  }
  if (dropdownOpen) { document.addEventListener('keydown', handleEscape) }
  return () => { document.removeEventListener('keydown', handleEscape) }
}, [dropdownOpen])

// Logout handler — migrar para UserMenu
const handleLogout = () => {
  setDropdownOpen(false)
  logout()
  navigate('/login', { replace: true })
}

// Botão atual (SUBSTITUIR por <UserMenu /> em AppLayout):
<button aria-controls="user-menu" ...>
  <span>{user?.nome} ({tipoLabel})</span>
  ...
</button>
{dropdownOpen && (
  <div id="user-menu" role="menu" className="absolute right-0 mt-2 w-40 ...">
    <button onClick={handleLogout} ...>Sair</button>
  </div>
)}
```

From frontend/src/components/admin/Sidebar.tsx (padrão de iniciais — copiar):
```ts
const initials = user?.nome
  ? user.nome.split(' ').slice(0, 2).map((n) => n[0]).join('').toUpperCase()
  : '?'
```

From frontend/src/contexts/AuthContext.tsx (hook):
```ts
const { user, logout } = useAuth()
// user: { nome: string, email: string, tipo: string } | null
// logout: () => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente UserMenu reutilizável</name>
  <files>frontend/src/components/UserMenu.tsx</files>
  <read_first>
    - frontend/src/components/AppLayout.tsx (lógica de dropdown completa a ser extraída — ler inteiro)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-03 (JSX exato do trigger, dropdown panel, ARIA, comportamentos)
  </read_first>
  <action>
Criar novo arquivo `frontend/src/components/UserMenu.tsx`. Este componente extrai TODA a lógica de dropdown do AppLayout e adiciona avatar com iniciais.

Conteúdo completo do arquivo:

```tsx
import { useState, useEffect, useRef, useMemo } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../contexts/AuthContext'

const tipoLabelMap: Record<string, string> = {
  admin: 'Admin',
  professor: 'Professor',
  responsavel: 'Responsável',
}

export function UserMenu() {
  const { user, logout } = useAuth()
  const navigate = useNavigate()
  const [dropdownOpen, setDropdownOpen] = useState(false)
  const dropdownRef = useRef<HTMLDivElement>(null)

  const initials = user?.nome
    ? user.nome.split(' ').slice(0, 2).map((n) => n[0]).join('').toUpperCase()
    : '?'

  const tipoLabel = useMemo(() => {
    return user?.tipo ? tipoLabelMap[user.tipo] ?? user.tipo : ''
  }, [user?.tipo])

  // Fechar ao clicar fora
  useEffect(() => {
    function handleClickOutside(event: MouseEvent) {
      if (
        dropdownRef.current &&
        !dropdownRef.current.contains(event.target as Node)
      ) {
        setDropdownOpen(false)
      }
    }
    if (dropdownOpen) {
      document.addEventListener('mousedown', handleClickOutside)
    }
    return () => {
      document.removeEventListener('mousedown', handleClickOutside)
    }
  }, [dropdownOpen])

  // Fechar ao pressionar Escape
  useEffect(() => {
    function handleEscape(event: KeyboardEvent) {
      if (event.key === 'Escape') {
        setDropdownOpen(false)
      }
    }
    if (dropdownOpen) {
      document.addEventListener('keydown', handleEscape)
    }
    return () => {
      document.removeEventListener('keydown', handleEscape)
    }
  }, [dropdownOpen])

  const handleLogout = () => {
    setDropdownOpen(false)
    logout()
    navigate('/login', { replace: true })
  }

  return (
    <div className="relative" ref={dropdownRef}>
      <button
        onClick={() => setDropdownOpen((prev) => !prev)}
        className="flex items-center gap-2 px-3 py-2 rounded-md text-sm font-semibold text-gray-700 hover:bg-gray-100 transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500"
        aria-haspopup="menu"
        aria-expanded={dropdownOpen}
        aria-controls="user-menu-dropdown"
      >
        {/* Avatar círculo com iniciais */}
        <span className="w-8 h-8 rounded-full bg-indigo-500/20 text-indigo-400 flex items-center justify-center text-xs font-semibold flex-shrink-0 select-none">
          {initials}
        </span>
        {/* Nome + tipo — oculto em telas muito pequenas */}
        <span className="hidden sm:flex flex-col items-start leading-tight">
          <span className="text-sm font-semibold text-gray-900 max-w-[120px] truncate">
            {user?.nome}
          </span>
          <span className="text-xs text-gray-500">{tipoLabel}</span>
        </span>
        {/* Chevron animado */}
        <svg
          className={`w-4 h-4 text-gray-400 transition-transform ${dropdownOpen ? 'rotate-180' : ''}`}
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      {dropdownOpen && (
        <div
          id="user-menu-dropdown"
          role="menu"
          className="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg border border-gray-200 py-1 z-50"
        >
          {/* Linha de identidade — não interativa */}
          <div className="px-4 py-2 border-b border-gray-100">
            <p className="text-sm font-semibold text-gray-900 truncate">{user?.nome}</p>
            <p className="text-xs text-gray-500">{tipoLabel}</p>
          </div>
          {/* Logout */}
          <button
            onClick={handleLogout}
            className="block w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-gray-50 transition-colors focus:outline-none focus:bg-gray-50"
            role="menuitem"
          >
            Sair
          </button>
        </div>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/UserMenu.tsx existe
    - frontend/src/components/UserMenu.tsx contém `export function UserMenu`
    - frontend/src/components/UserMenu.tsx contém `w-8 h-8 rounded-full bg-indigo-500/20 text-indigo-400`
    - frontend/src/components/UserMenu.tsx contém `id="user-menu-dropdown"`
    - frontend/src/components/UserMenu.tsx contém `aria-haspopup="menu"`
    - frontend/src/components/UserMenu.tsx contém `role="menu"`
    - frontend/src/components/UserMenu.tsx contém `role="menuitem"`
    - frontend/src/components/UserMenu.tsx contém `handleClickOutside` (click outside handler)
    - frontend/src/components/UserMenu.tsx contém `handleEscape` (escape key handler)
    - frontend/src/components/UserMenu.tsx contém `text-red-600` (logout button)
    - frontend/src/components/UserMenu.tsx contém `rotate-180` (chevron animation)
    - frontend/src/components/UserMenu.tsx contém `w-48` (dropdown width)
    - TypeScript compila sem erros em UserMenu.tsx
  </acceptance_criteria>
  <done>UserMenu.tsx existe com avatar de iniciais indigo, dropdown com identidade (nome + tipo) e botão Sair, lógica de click-outside e Escape, ARIA correto, exportado como named export UserMenu.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar AppLayout para usar UserMenu no lugar do botão textual</name>
  <files>frontend/src/components/AppLayout.tsx</files>
  <read_first>
    - frontend/src/components/AppLayout.tsx (leitura obrigatória — ver estado atual inteiro antes de editar)
    - frontend/src/components/UserMenu.tsx (o componente recém criado — confirmar export name)
    - .planning/phases/08-ux-polish/08-UI-SPEC.md §C-03 último parágrafo "AppLayout upgrade (D-05)"
  </read_first>
  <action>
Atualizar AppLayout.tsx para usar `<UserMenu />` no lugar de todo o bloco do dropdown atual. AppLayout vira uma thin shell que delega a lógica de autenticação/dropdown ao UserMenu.

Mudanças:
1. Adicionar import: `import { UserMenu } from './UserMenu'`
2. Remover imports que eram usados apenas para o dropdown e que agora vivem em UserMenu: `useEffect`, `useRef`, `useMemo` — manter apenas `useState` se ainda usado, `Outlet`, `Link`, `useNavigate`, `useAuth` (verificar se algum ainda é usado no AppLayout para outra coisa; se não for, remover)
3. Remover estado `dropdownOpen`, ref `dropdownRef`, computed `tipoLabel`
4. Remover os dois `useEffect` de click-outside e escape
5. Remover `handleLogout`
6. Substituir o bloco `<div className="relative" ref={dropdownRef}>...</div>` por `<UserMenu />`

Estado final esperado do header do AppLayout:
```tsx
{/* User menu */}
<UserMenu />
```

O AppLayout deve ficar sem nenhum estado local (se não houver outra lógica que use useState) e sem os dois useEffects de dropdown.

IMPORTANTE: Manter:
- Skip link `<a href="#conteudo-principal" ...>Ir para o conteúdo principal</a>`
- Header com classes `fixed top-0 left-0 right-0 z-50 bg-white shadow-sm border-b border-gray-200`
- Inner div `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between`
- Logo `<Link to={...}>Sistema Escolar</Link>`
- `<main id="conteudo-principal" className="flex-1 pt-16">`
- `<Outlet />`

Resultado: AppLayout.tsx fica com ~25 linhas (vs 128 originais) pois delega toda a lógica de usuário ao UserMenu.
  </action>
  <verify>
    <automated>cd /Users/paulosouza/projetos/projeto-integrador-PJI110-1/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/AppLayout.tsx contém `import { UserMenu } from './UserMenu'`
    - frontend/src/components/AppLayout.tsx contém `<UserMenu />`
    - frontend/src/components/AppLayout.tsx NÃO contém `dropdownOpen` (estado removido)
    - frontend/src/components/AppLayout.tsx NÃO contém `dropdownRef` (ref removida)
    - frontend/src/components/AppLayout.tsx NÃO contém `id="user-menu"` (antigo id — substituído por user-menu-dropdown no UserMenu)
    - frontend/src/components/AppLayout.tsx contém `id="conteudo-principal"` (preservado)
    - frontend/src/components/AppLayout.tsx contém `Ir para o conteúdo principal` (skip link preservado)
    - frontend/src/components/AppLayout.tsx contém `pt-16` no main (clearance para header fixo)
    - TypeScript compila sem erros em AppLayout.tsx
  </acceptance_criteria>
  <done>AppLayout.tsx usa UserMenu no header, removeu toda lógica de dropdown (delegada ao UserMenu), preservou skip link, logo, estrutura do header e main com pt-16, TypeScript compilando sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→auth | user.nome e user.tipo vêm do JWT já validado pelo backend — apenas display, sem nova chamada de rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | UserMenu dropdown (nome + tipo exibidos) | accept | Informação do usuário já estava exposta na UI anterior; apenas nome e tipo de perfil — sem dados sensíveis como email ou CPF no dropdown |
| T-08-04 | Spoofing | handleLogout no UserMenu | accept | Chama `logout()` do AuthContext que limpa localStorage + `navigate('/login')` — padrão idêntico ao AppLayout original |
</threat_model>

<verification>
Após execução das duas tasks:

```bash
cd frontend && npx tsc --noEmit
```

Deve sair com código 0.

Verificação manual mínima:
1. Login como professor → header do AppLayout mostra círculo indigo com iniciais + nome + tipo
2. Clicar no avatar → dropdown abre com nome completo, tipo e botão "Sair"
3. Pressionar Escape → dropdown fecha
4. Clicar fora do dropdown → dropdown fecha
5. Clicar "Sair" → redireciona para /login
6. Login como responsável → mesmo comportamento
7. Login como admin → header do AdminLayout mostra UserMenu (após 08-01 ser executado)
</verification>

<success_criteria>
- UserMenu.tsx existe e exporta named export `UserMenu`
- Avatar círculo com classes `w-8 h-8 rounded-full bg-indigo-500/20 text-indigo-400`
- Dropdown com `id="user-menu-dropdown"`, `role="menu"`, identidade (nome + tipo) e botão Sair em text-red-600
- AppLayout usa `<UserMenu />` — sem duplicação de lógica de dropdown
- `npm run build` no frontend passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-ux-polish/08-02-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
