---
phase: 07-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
tags:
  - deploy
  - infrastructure
  - smoke-test

user_setup:
  - service: render
    why: "Host backend FastAPI as a Web Service (free tier) with native GitHub auto-deploy (D-05)"
    env_vars:
      - name: DATABASE_URL
        source: "Set in render.yaml — value: sqlite:///./escola.db (no manual action)"
      - name: ENVIRONMENT
        source: "Set in render.yaml — value: production (no manual action)"
      - name: SECRET_KEY
        source: "Auto-generated by Render on first deploy (generateValue: true)"
      - name: ACCESS_TOKEN_EXPIRE_DAYS
        source: "Set in render.yaml — value: 7 (no manual action)"
      - name: CORS_ORIGINS
        source: "Render Dashboard → Service → Environment → set to JSON array string of CF Pages URL, e.g. [\"https://escola-pi.pages.dev\"]"
      - name: FRONTEND_URL
        source: "Render Dashboard → Service → Environment → set to https://<your-cf-pages-project>.pages.dev"
      - name: SMTP_HOST
        source: "Render Dashboard → Service → Environment → sandbox.smtp.mailtrap.io"
      - name: SMTP_PORT
        source: "Render Dashboard → Service → Environment → 587"
      - name: SMTP_USER
        source: "Mailtrap Dashboard → Email Testing → Inboxes → SMTP Settings → Username"
      - name: SMTP_PASS
        source: "Mailtrap Dashboard → Email Testing → Inboxes → SMTP Settings → Password"
      - name: SMTP_SENDER
        source: "Render Dashboard → Service → Environment → noreply@escola.dev"
    dashboard_config:
      - task: "Create new Render account (if needed) and connect GitHub repository"
        location: "render.com → Sign in with GitHub → New + → Blueprint → select this repo"
      - task: "Confirm Render reads render.yaml at repo root and creates the escola-api Web Service"
        location: "Render Dashboard → Blueprints → review escola-api"
      - task: "After CF Pages URL is known (set up below), set CORS_ORIGINS and FRONTEND_URL env vars"
        location: "Render Dashboard → escola-api → Environment → Add Environment Variable"
      - task: "Trigger redeploy after env vars are set (Manual Deploy → Deploy latest commit)"
        location: "Render Dashboard → escola-api → Manual Deploy"
  - service: cloudflare_pages
    why: "Host frontend React SPA with global CDN and native GitHub auto-deploy (D-03, D-05)"
    env_vars:
      - name: VITE_API_URL
        source: "Cloudflare Pages Dashboard → Project → Settings → Environment variables (Production) → set to https://<your-render-service>.onrender.com"
    dashboard_config:
      - task: "Create new Cloudflare account (if needed) and authorize GitHub access"
        location: "dash.cloudflare.com → Workers & Pages → Create → Pages → Connect to Git"
      - task: "Create Pages project pointing to the GitHub repo"
        location: "Cloudflare Pages → Connect to Git → select repo → Begin setup"
      - task: "Configure build settings — Framework: None / Vite (or React); Build command: npm run build; Build output directory: dist; Root directory: frontend"
        location: "Cloudflare Pages → project setup → Build settings"
      - task: "Set VITE_API_URL env var (Production) to the Render service URL BEFORE first build"
        location: "Cloudflare Pages → Settings → Environment variables → Production → Add"
      - task: "Trigger first deploy (or rebuild after VITE_API_URL is set if first build went out without it)"
        location: "Cloudflare Pages → Deployments → Retry deployment"
  - service: mailtrap
    why: "SMTP sandbox for password-reset emails in production (already used in dev); credentials feed Render env vars"
    env_vars:
      - name: SMTP_USER
        source: "Mailtrap Dashboard → Email Testing → Inboxes → My Inbox → SMTP Settings → Username"
      - name: SMTP_PASS
        source: "Mailtrap Dashboard → Email Testing → Inboxes → My Inbox → SMTP Settings → Password"
    dashboard_config:
      - task: "Reuse existing Mailtrap sandbox inbox or create one if not yet provisioned"
        location: "mailtrap.io → Email Testing → Inboxes"

must_haves:
  truths:
    - "Backend `/health` endpoint returns 200 at the public Render URL"
    - "Frontend loads at the public Cloudflare Pages URL without console errors"
    - "Login with admin@escola.dev / Admin@123 succeeds and admin is redirected to admin dashboard"
    - "Admin dashboard shows the seeded turmas (2), alunos (8), and disciplinas (3)"
    - "Login with prof.silva@escola.dev / Prof@123 shows Maria Silva's turma A with 3 disciplinas and 4 alunos"
    - "Login with resp.oliveira@escola.dev / Resp@123 shows boletim of the linked alunos with grades and frequency"
    - "Direct URL navigation to /admin or /professor on the deployed site does NOT 404 (SPA fallback works)"
    - "A push to master triggers a new run in the GitHub Actions tab and a new deploy in Render + Cloudflare dashboards"
  artifacts:
    - path: ".planning/phases/07-deploy/07-02-SUMMARY.md"
      provides: "Deploy execution log including final URLs and smoke-test results"
      contains: "BACKEND_URL"
    - path: ".planning/phases/07-deploy/07-02-SUMMARY.md"
      provides: "Frontend deploy URL and CORS config used"
      contains: "FRONTEND_URL"
  key_links:
    - from: "render.yaml (committed in Plan 01)"
      to: "Render Web Service escola-api"
      via: "Render Blueprint sync (autoDeploy: true)"
      pattern: "rootDir: backend"
    - from: "Cloudflare Pages project"
      to: "frontend/public/_redirects (committed in Plan 01)"
      via: "Vite build copies public/* to dist/, CF Pages serves dist/"
      pattern: "/index.html"
    - from: "Frontend SPA"
      to: "Backend FastAPI"
      via: "axios baseURL = import.meta.env.VITE_API_URL (CORS allow-list set in Render)"
      pattern: "VITE_API_URL"
    - from: "Push to master on GitHub"
      to: "Render + Cloudflare deploys"
      via: "Native platform webhooks (D-05)"
      pattern: "branches: [master]"
---

<objective>
Configure the Render and Cloudflare Pages platforms so the artifacts produced in Plan 01 result in a publicly-accessible production deployment, and verify end-to-end that the system works for all three user profiles (admin, professor, responsável).

Purpose: Phase 7 success criteria require live URLs — both backend and frontend must be reachable on the public internet, and a push to master must trigger an auto-deploy. This plan is the platform-side counterpart of Plan 01 and contains every action that must happen outside the repository.

Output: Two live URLs (Render + Cloudflare Pages), env vars configured on both platforms, demonstrated auto-deploy via push, and a SUMMARY.md capturing the URLs + smoke-test results for the academic report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/07-deploy/07-RESEARCH.md
@.planning/phases/07-deploy/07-VALIDATION.md
@.planning/phases/07-deploy/07-01-PLAN.md
@render.yaml
@frontend/public/_redirects
@.github/workflows/deploy.yml
@backend/alembic/versions/0002_seed_demo.py
@backend/.env.example

<note>
This plan is autonomous: false. Most steps require the human operator (paulo225vitor@gmail.com) to interact with Render, Cloudflare, and Mailtrap dashboards because Claude has no API credentials and no way to authenticate to those platforms. Claude's role here is to (a) commit and push the artifacts from Plan 01, (b) provide step-by-step instructions, (c) capture results in the SUMMARY at the end.
</note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit Plan 01 artifacts and push to master to trigger first deploy</name>
  <files>(no source files modified — git operation only)</files>
  <read_first>
    - render.yaml (verify exists and contains rootDir: backend before committing)
    - .github/workflows/deploy.yml (verify exists and triggers on master)
    - frontend/public/_redirects (verify exists)
    - backend/alembic/versions/0002_seed_demo.py (verify exists, chains from 0001)
    - .planning/phases/07-deploy/07-01-SUMMARY.md (confirm Plan 01 was completed)
  </read_first>
  <behavior>
    - Test: `git status` after the commit shows a clean working tree (no untracked Plan 01 artifacts).
    - Test: `git log -1 --oneline` shows the new commit on master.
    - Test: After `git push`, the head commit reaches origin/master — verified manually via GitHub Actions tab (Task 5) and Render/CF dashboard activity (Tasks 2 and 3). Not asserted in the automated verify block to avoid network dependency.
  </behavior>
  <action>
    Stage and commit the four files produced by Plan 01, plus the two SUMMARY files (07-01-SUMMARY.md and the placeholder for 07-02 if pre-created), and push to master to trigger the native auto-deploy webhooks (Render + Cloudflare Pages).

    Run these commands in order from the repo root:

    1. Verify Plan 01 artifacts are present and unmodified since Plan 01 completed:
       ```bash
       git status --short
       ```
       Expected: untracked entries for `render.yaml`, `.github/workflows/deploy.yml`, `frontend/public/_redirects`, `backend/alembic/versions/0002_seed_demo.py`, and `.planning/phases/07-deploy/07-01-SUMMARY.md`.

    2. Stage exactly these files (do not use `git add -A`):
       ```bash
       git add render.yaml .github/workflows/deploy.yml frontend/public/_redirects backend/alembic/versions/0002_seed_demo.py .planning/phases/07-deploy/07-01-SUMMARY.md
       ```

    3. Create the commit with an exact message (HEREDOC, no editor):
       ```bash
       git commit -m "$(cat <<'EOF'
       feat(07): add deploy artifacts (render.yaml, _redirects, deploy workflow, seed migration)

       Plan 07-01 — infrastructure artifacts for production deploy:
       - render.yaml: Render Blueprint, rootDir backend, compound startCommand
         (preDeployCommand replaced because free tier limitation)
       - .github/workflows/deploy.yml: documentation workflow (D-06)
       - frontend/public/_redirects: SPA fallback for CF Pages (D-04)
       - backend/alembic/versions/0002_seed_demo.py: rich demo seed (D-02)

       Closes part of DEPLOY-01 (artifact side).

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
       EOF
       )"
       ```

    4. Push to master to fire the platform webhooks:
       ```bash
       git push origin master
       ```

    5. After the push, capture the commit hash for the SUMMARY:
       ```bash
       git rev-parse HEAD
       ```
       Save this hash; Task 5 records it.

    DO NOT push if the working tree contains unrelated modifications. If `git status` shows changes outside the four Plan 01 files + the SUMMARY, STOP and surface a checkpoint to the operator. DO NOT use `--no-verify` to bypass any pre-commit hooks. DO NOT force-push.
  </action>
  <verify>
    <!-- Note: post-push remote-state check (git ls-remote) is intentionally NOT in the automated block to avoid network dependency in disconnected environments. The acceptance_criteria GitHub Actions tab check (Task 5) plus Render/CF dashboard activity (Tasks 2 and 3) prove the push reached origin. -->
    <automated>git status --porcelain | grep -E '^(\?\?|\sM|M )' | grep -E '(render\.yaml|\.github/workflows/deploy\.yml|frontend/public/_redirects|backend/alembic/versions/0002_seed_demo\.py)' ; test "$(git status --porcelain | grep -E '(render\.yaml|\.github/workflows/deploy\.yml|frontend/public/_redirects|backend/alembic/versions/0002_seed_demo\.py)' | wc -l | tr -d ' ')" = "0" &amp;&amp; git log -1 --oneline | grep -F 'feat(07)'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` does NOT list any of: render.yaml, .github/workflows/deploy.yml, frontend/public/_redirects, backend/alembic/versions/0002_seed_demo.py (i.e. all four are committed).
    - `git log -1 --pretty=%s` returns a subject containing `feat(07)` and `deploy artifacts`.
    - Push succeeded — verified manually via the GitHub Actions tab (a new "Deploy" workflow run appears for the head commit; covered in Task 5) and via Render/CF dashboard activity (Tasks 2 and 3). The `git ls-remote` check is intentionally omitted from the automated verify block to avoid network dependency.
    - GitHub Actions tab shows a new "Deploy" workflow run for this commit (verified manually in Task 5).
  </acceptance_criteria>
  <done>The four artifacts are on origin/master, GitHub Actions has fired, and Render + Cloudflare Pages webhooks have been triggered.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure Render Web Service (manual — platform UI required)</name>
  <what-built>
    Plan 01 produced `render.yaml` at repo root. Task 1 of this plan pushed it to master, which fires Render's native GitHub webhook. Render now needs an account, a connected repository, and the dashboard-only env vars filled in.
  </what-built>
  <how-to-verify>
    Perform every step below in order. Do NOT skip.

    Account + Blueprint setup:
    1. Open https://render.com and sign in with the GitHub account that owns this repository (paulo225vitor@gmail.com or whichever account has push access).
    2. From the dashboard, click "New +" → "Blueprint".
    3. Select the GitHub repository for this project. Render reads `render.yaml` from the repo root and shows a preview of the service `escola-api`.
    4. Click "Apply" / "Create New Resources". Render creates the Web Service. The first build will FAIL or show "missing env vars" because CORS_ORIGINS and FRONTEND_URL are not yet set — that is expected at this stage. Continue.

    Dashboard env vars (the ones marked `sync: false` in render.yaml):
    5. Open the new `escola-api` service → Environment tab.
    6. Confirm the following keys already exist (copied from render.yaml): DATABASE_URL=sqlite:///./escola.db, ENVIRONMENT=production, ACCESS_TOKEN_EXPIRE_DAYS=7, SECRET_KEY (auto-generated, do not change).
    7. Add these env vars (DO NOT yet set CORS_ORIGINS or FRONTEND_URL — that happens in Task 4 after the CF Pages URL is known):
       - SMTP_HOST = `sandbox.smtp.mailtrap.io`
       - SMTP_PORT = `587`
       - SMTP_USER = (paste from Mailtrap → Inbox → SMTP Settings → Username)
       - SMTP_PASS = (paste from Mailtrap → Inbox → SMTP Settings → Password)
       - SMTP_SENDER = `noreply@escola.dev`
    8. Click "Save Changes". Render will trigger a redeploy automatically.

    Capture URLs (record these for the SUMMARY):
    9. Wait until the service status reads "Live" (green) — typically 2-5 minutes for the first build, longer if pip install takes time.
    10. Copy the service URL shown at the top of the page (format: `https://<service-name>-<hash>.onrender.com` or `https://escola-api.onrender.com`). Record this as `BACKEND_URL` for the SUMMARY.

    Smoke test (backend only — no frontend yet):
    11. From a terminal: `curl -i $BACKEND_URL/health` (replace $BACKEND_URL). Expected response: HTTP 200 with body `{"status":"ok","environment":"production"}`. NOTE: First request after long idle takes 30-60s on free tier (cold start) — this is normal. Retry once if first request times out.
    12. Confirm seed migration ran by hitting an admin endpoint. From a terminal:
        ```bash
        curl -X POST $BACKEND_URL/auth/login \
          -H "Content-Type: application/json" \
          -d '{"email":"admin@escola.dev","password":"Admin@123"}'
        ```
        Expected: HTTP 200 with a JSON body containing `access_token` (a JWT). If you receive 401, the seed admin row is wrong (escalate). If you receive a CORS error here, that is fine — curl doesn't enforce CORS; this only tests the backend.

    If any step fails:
    - "Build failed" in Render logs → check Logs tab. The most common cause is `alembic upgrade head` failure due to wrong DATABASE_URL or import path. Confirm DATABASE_URL is `sqlite:///./escola.db` (relative path; rootDir is backend).
    - 503 / cold-start hang for >2 min → wait, retry once. If persistent, escalate.
    - 401 on admin login → seed migration didn't run; check Render logs for alembic output.

    Record the BACKEND_URL exactly as shown in the Render dashboard. It will be used in Tasks 3 and 4.
  </how-to-verify>
  <resume-signal>Type "approved" with the BACKEND_URL pasted, e.g. `approved BACKEND_URL=https://escola-api.onrender.com`. If you hit problems, describe them and we will troubleshoot.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Configure Cloudflare Pages project (manual — platform UI required)</name>
  <what-built>
    Plan 01 produced `frontend/public/_redirects` and confirmed `npm run build` works. Task 1 pushed it. Cloudflare Pages now needs an account, the repo connected, build settings entered, and `VITE_API_URL` set to the BACKEND_URL captured in Task 2.
  </what-built>
  <how-to-verify>
    Perform every step in order.

    Account + project setup:
    1. Open https://dash.cloudflare.com and sign in (create a free account if needed).
    2. Navigate to "Workers & Pages" → "Create" → "Pages" → "Connect to Git".
    3. Authorize Cloudflare to access your GitHub account, then select the repository for this project.
    4. On the "Set up builds and deployments" page, configure EXACTLY these values:
       - Production branch: `master`
       - Framework preset: `None` (or `Vite` — both work, but `None` matches the manual command below)
       - Build command: `npm run build`
       - Build output directory: `dist`
       - Root directory: `frontend`  (CRITICAL — without this, the build runs from repo root and fails)

    Set the API URL env var (must be set before first build, otherwise the bundle is built without it):
    5. Before clicking "Save and Deploy", expand "Environment variables (advanced)".
    6. Add a Production env var:
       - Variable name: `VITE_API_URL`
       - Value: `<BACKEND_URL from Task 2>` (e.g. `https://escola-api.onrender.com`) — NO trailing slash.
    7. Click "Save and Deploy". CF Pages runs `cd frontend && npm install && npm run build` and serves `dist/` on a `*.pages.dev` URL.

    Capture URL:
    8. Wait until the deployment status shows "Success" (typically 1-3 minutes).
    9. Copy the assigned URL from the "Production" panel (format: `https://<project-name>.pages.dev` — e.g. `https://escola-pi.pages.dev`). Record this as `FRONTEND_URL` for the SUMMARY.

    Sanity smoke test (frontend loads, but login will still fail until Task 4):
    10. Open `$FRONTEND_URL` in a browser. The login page should render (split-screen, "Entrar" form). React Router should be working — navigate manually to `$FRONTEND_URL/login` and confirm no 404.
    11. Open browser DevTools → Network tab. The page itself loads from `*.pages.dev`. There are no API calls on the login page yet, so the broken CORS in step (still pending Task 4) does not matter.
    12. Test SPA fallback by typing `$FRONTEND_URL/admin/alunos` directly into the address bar. The page should load (and likely show the login redirect because no auth) — NOT a 404 from CF Pages. If you get a 404 page from Cloudflare, _redirects did not deploy correctly; verify `frontend/public/_redirects` was committed (Task 1) and that the build output `dist/_redirects` exists in the latest deployment artifact.

    If steps fail:
    - 404 from Cloudflare on root: check that "Root directory" is `frontend` and "Build output directory" is `dist`.
    - Build fails with TypeScript errors: re-run `cd frontend && npm run build` locally; if local build fails, the issue is in the codebase, not the deploy.
    - VITE_API_URL undefined in built bundle: open DevTools console on the deployed page and run `import.meta.env` — if VITE_API_URL is missing, the env var was not set BEFORE the build. Set it in CF Pages Settings → Environment variables, then trigger a Retry deployment.

    Record the FRONTEND_URL exactly as shown.
  </how-to-verify>
  <resume-signal>Type "approved" with the FRONTEND_URL, e.g. `approved FRONTEND_URL=https://escola-pi.pages.dev`. If you hit problems, describe them.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Set CORS_ORIGINS and FRONTEND_URL on Render, redeploy, and verify cross-origin login</name>
  <what-built>
    Backend is live (Task 2). Frontend is live (Task 3). Now we need to wire them together: tell the backend to accept requests from the CF Pages URL (CORS_ORIGINS) and tell the password-reset emails to point to the CF Pages URL (FRONTEND_URL).
  </what-built>
  <how-to-verify>
    1. Open Render Dashboard → escola-api service → Environment tab.
    2. Add (or update) these two env vars using the FRONTEND_URL captured in Task 3:
       - `CORS_ORIGINS` = `["https://<your-cf-pages-project>.pages.dev"]`
         CRITICAL FORMATTING: this MUST be a JSON array string. Single line. Double quotes around the URL. Example exact value: `["https://escola-pi.pages.dev"]`. Pydantic-settings parses this as `list[str]`. NO trailing slash on the URL itself.
       - `FRONTEND_URL` = `https://<your-cf-pages-project>.pages.dev` (plain string, no quotes, no brackets, no trailing slash). Example: `https://escola-pi.pages.dev`.
    3. Click "Save Changes". Render triggers an automatic redeploy. Wait for status "Live" (~2-3 min).
    4. Cross-origin login smoke test (browser):
       a. Open `$FRONTEND_URL` (your CF Pages URL).
       b. On the login form, enter `admin@escola.dev` / `Admin@123` and submit.
       c. Expected: login succeeds, browser redirects to admin dashboard URL (e.g. `$FRONTEND_URL/admin`), no CORS errors in DevTools console.
       d. Navigate to "Alunos" — confirm the 8 seeded alunos appear (Pedro Oliveira, Lucas Santos, Júlia Oliveira, Bruno Santos, Sofia Oliveira, Mateus Santos, Laura Oliveira, Gabriel Santos).
       e. Navigate to "Turmas" — confirm 2 turmas appear ("Turma A - 9º Ano", "Turma B - 8º Ano").
       f. Navigate to "Disciplinas" — confirm 3 (Matemática, Português, Ciências).
       g. Navigate to "Professores" — confirm 2 (Maria Silva, João Souza).
       h. Navigate to "Responsáveis" — confirm 2 (Ana Oliveira, Carlos Santos).
    5. Professor smoke test:
       a. Logout. Login with `prof.silva@escola.dev` / `Prof@123`.
       b. Confirm Maria Silva sees "Turma A - 9º Ano" with 4 alunos and the 3 disciplinas.
       c. Open the chamada tab — confirm at least 2 chamadas exist (datas 2026-03-10 and 2026-03-24).
       d. Open the notas tab — confirm grades exist for all 4 bimestres for each disciplina.
    6. Responsável smoke test:
       a. Logout. Login with `resp.oliveira@escola.dev` / `Resp@123`.
       b. Confirm Ana Oliveira sees boletim with at least one filho (Pedro Oliveira, Júlia Oliveira, Sofia Oliveira, or Laura Oliveira).
       c. Confirm grades and frequency display correctly.
    7. CORS-failure sanity test (negative — confirm CORS is not wide open):
       a. From DevTools console on `$FRONTEND_URL`, run:
          ```js
          fetch('$BACKEND_URL/health').then(r => r.json()).then(console.log)
          ```
          Replace `$BACKEND_URL` with the literal Render URL. Expected: succeeds (the same origin is allow-listed).
       b. From DevTools console on a different origin (e.g. https://example.com — open in a new tab and run from its console), the same fetch should FAIL with a CORS error. This is optional but a useful negative check.

    If any smoke test fails:
    - "CORS error" on login → CORS_ORIGINS env var is wrong format. Must be `["https://...pages.dev"]` (JSON array), not `https://...pages.dev` (plain string). Double-check exact characters.
    - 401 on login → check that bcrypt hashes in seed migration were generated correctly. From local: `cd backend && python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).verify('Admin@123', '<hash from migration 0001>'))"` — must print True.
    - "Network error" / fetch fails → BACKEND_URL not reachable. Confirm Render service is "Live", not "Suspended".
    - Empty admin tables → seed migration didn't run. Check Render Logs tab for `alembic upgrade head` output. Look for a line like `Running upgrade 0001 -> 0002, seed demo data`.
  </how-to-verify>
  <resume-signal>Type "approved" with a 1-line confirmation per smoke test, e.g. `approved admin/professor/responsavel logins all work, seed visible`. If anything failed, describe.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Trigger auto-deploy via push to master and verify all three platforms react</name>
  <what-built>
    Render and Cloudflare Pages are both configured with autoDeploy on master. We now confirm that a fresh push triggers (a) GitHub Actions, (b) Render rebuild, (c) Cloudflare Pages rebuild — closing the "deploy automático via GitHub Actions" success criterion of Phase 7.
  </what-built>
  <how-to-verify>
    Make a trivial documentation-only change and push it. The change must be small enough that it cannot break either deploy.

    1. From repo root, create a tiny no-op edit to PROJECT.md or README.md:
       ```bash
       date -u +"Last verified deploy: %Y-%m-%dT%H:%M:%SZ" >> .planning/PROJECT.md
       git add .planning/PROJECT.md
       git commit -m "chore(07): trigger deploy verification ($(date -u +%Y-%m-%d))"
       git push origin master
       ```
       (If you prefer not to touch PROJECT.md, an empty commit also works: `git commit --allow-empty -m "chore(07): trigger deploy verification"`. Empty commits are still pushed and still fire webhooks.)

    2. Open three tabs:
       - GitHub repo → Actions tab. Within ~30s of the push, a new "Deploy" workflow run appears for the new commit. Wait for it to complete (~10s, just an echo step). Status should be green.
       - Render dashboard → escola-api → Events. Within ~1-3 min, a new deploy event appears for the new commit. Wait for status "Live".
       - Cloudflare Pages → project → Deployments. Within ~1-3 min, a new "Production" deployment appears. Wait for "Success".

    3. After all three complete, re-test the live system:
       - `curl -i $BACKEND_URL/health` returns 200 (Render survived the redeploy).
       - Open `$FRONTEND_URL` in a browser, login as admin again, confirm seeded data still visible (CF Pages survived the redeploy; Render seed re-ran on backend redeploy because DB is ephemeral — verify alunos count is still 8).

    4. Capture for the SUMMARY:
       - Final commit hash that triggered this verification deploy.
       - Timestamps (or just durations) for each platform's deploy.
       - Screenshot or text confirmation of all three platforms showing green status.
  </how-to-verify>
  <resume-signal>Type "approved" with the verification commit hash, e.g. `approved commit=abc1234, all three platforms green`. If any platform failed to react, describe.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Write 07-02-SUMMARY.md capturing live URLs, env vars, and smoke-test results</name>
  <files>.planning/phases/07-deploy/07-02-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY format)
    - .planning/phases/07-deploy/07-01-SUMMARY.md (matching tone and section structure)
    - .planning/phases/07-deploy/07-CONTEXT.md (decisions D-01 through D-07 to reference in summary)
    - .planning/phases/07-deploy/07-RESEARCH.md (note D-07 supersession)
  </read_first>
  <behavior>
    - Test: SUMMARY file exists, has frontmatter, contains both BACKEND_URL and FRONTEND_URL exactly as captured.
    - Test: SUMMARY documents the D-07 deviation (preDeployCommand → compound startCommand) and references RESEARCH.md.
    - Test: SUMMARY lists smoke-test results for all three user profiles.
  </behavior>
  <action>
    Create `.planning/phases/07-deploy/07-02-SUMMARY.md` capturing the live deploy state. Use the values collected from the resume signals of Tasks 2, 3, 4, and 5 (BACKEND_URL, FRONTEND_URL, verification commit hash).

    Follow this exact structure (replace `<placeholders>` with the captured values):

    ```markdown
    ---
    phase: 07-deploy
    plan: 02
    status: complete
    requirements:
      - DEPLOY-01
    completed: <YYYY-MM-DD from resume>
    ---

    # Plan 07-02 Summary — Live Deploy Verification

    ## Outcome

    Sistema implantado em produção. Backend FastAPI rodando no Render Web Service free tier, frontend React rodando no Cloudflare Pages free tier. Auto-deploy via webhooks nativos das plataformas confirmado por push de verificação ao final.

    ## Live URLs

    - **Backend (Render):** `<BACKEND_URL>`
      - Health check: `<BACKEND_URL>/health` → 200
      - Login endpoint: `<BACKEND_URL>/auth/login`
    - **Frontend (Cloudflare Pages):** `<FRONTEND_URL>`
      - Login: `<FRONTEND_URL>/login`
      - Admin dashboard: `<FRONTEND_URL>/admin`

    ## Env vars configured

    Render Dashboard (escola-api → Environment):
    | Key | Source | Notes |
    |-----|--------|-------|
    | DATABASE_URL | render.yaml literal | sqlite:///./escola.db |
    | ENVIRONMENT | render.yaml literal | production |
    | SECRET_KEY | render.yaml generateValue | Generated by Render on first deploy |
    | ACCESS_TOKEN_EXPIRE_DAYS | render.yaml literal | 7 |
    | CORS_ORIGINS | dashboard | `["<FRONTEND_URL>"]` (JSON array string) |
    | FRONTEND_URL | dashboard | `<FRONTEND_URL>` |
    | SMTP_HOST | dashboard | sandbox.smtp.mailtrap.io |
    | SMTP_PORT | dashboard | 587 |
    | SMTP_USER | dashboard | (Mailtrap inbox username — not recorded here) |
    | SMTP_PASS | dashboard | (Mailtrap inbox password — not recorded here) |
    | SMTP_SENDER | dashboard | noreply@escola.dev |

    Cloudflare Pages Dashboard (project → Settings → Environment variables → Production):
    | Key | Value |
    |-----|-------|
    | VITE_API_URL | `<BACKEND_URL>` |

    ## Smoke-test results

    Performed against the live URLs after Task 4 completed:

    | Profile | Credential | Result |
    |---------|-----------|--------|
    | Admin | admin@escola.dev / Admin@123 | Login OK; 8 alunos / 2 turmas / 3 disciplinas / 2 prof / 2 resp visíveis |
    | Professor | prof.silva@escola.dev / Prof@123 | Login OK; vê Turma A com 4 alunos, 3 disciplinas, chamadas e notas dos 4 bimestres |
    | Responsável | resp.oliveira@escola.dev / Resp@123 | Login OK; boletim com notas e frequência calculadas |

    SPA fallback: direct navigation to `<FRONTEND_URL>/admin/alunos` does not 404 (CF Pages serves index.html via _redirects).

    ## Auto-deploy verification (Task 5)

    Verification commit: `<COMMIT_HASH>`
    - GitHub Actions: workflow "Deploy" ran and turned green within ~30s.
    - Render: redeploy fired automatically; status "Live" within ~<X> min. Seed re-applied (alunos count after redeploy = 8).
    - Cloudflare Pages: redeploy fired automatically; status "Success" within ~<X> min.

    Confirms Phase 7 success criterion: "Push para `master` dispara deploy automático via GitHub Actions" — fulfilled via native platform webhooks (D-05 / D-06).

    ## Deviations from plan

    - **D-07 (preDeployCommand)** — Replaced with compound `startCommand` (`bash -c "alembic upgrade head && uvicorn ..."`) because `preDeployCommand` requires a paid Render instance. Documented in 07-RESEARCH.md §"Critical discovered constraint" and 07-01-PLAN.md. Safety property is preserved: if migrations fail, uvicorn never starts → deploy is marked failed → previous live service stays up.

    ## DEPLOY-01 status

    Closed. All success criteria from ROADMAP.md §"Phase 7" met:
    1. ✅ Backend FastAPI acessível em URL pública no Render
    2. ✅ Frontend React acessível em URL pública (Cloudflare Pages)
    3. ✅ Push para `master` dispara deploy automático (GitHub Actions + native webhooks)
    4. ✅ Variáveis de ambiente configuradas via dashboards, não commitadas
    ```

    Save to `.planning/phases/07-deploy/07-02-SUMMARY.md`. DO NOT commit it from inside this task — committing happens in the orchestrator's post-phase routine. DO NOT include any actual SECRET_KEY value or SMTP password in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; grep -q '^---$' .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; grep -qE 'BACKEND_URL|onrender\.com' .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; grep -qE 'FRONTEND_URL|pages\.dev' .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; grep -q 'D-07' .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; grep -q 'compound' .planning/phases/07-deploy/07-02-SUMMARY.md &amp;&amp; ! grep -E '\$2b\$12\$|SMTP_PASS=[a-zA-Z0-9]' .planning/phases/07-deploy/07-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy/07-02-SUMMARY.md` exists.
    - File starts with frontmatter (`---` on first line; `phase: 07-deploy`, `plan: 02` present).
    - File contains the literal BACKEND_URL captured in Task 2 (recognizable by `onrender.com` substring or the explicit URL).
    - File contains the literal FRONTEND_URL captured in Task 3 (recognizable by `pages.dev` substring).
    - File contains a section discussing the D-07 deviation, mentioning "compound" or "preDeployCommand".
    - File does NOT contain any bcrypt hash (no `$2b$12$...` strings) and does NOT contain any plaintext SMTP password.
    - File documents smoke-test results for admin, professor, and responsável profiles.
    - File documents the verification commit hash from Task 5.
  </acceptance_criteria>
  <done>SUMMARY captures live URLs, env-var configuration, smoke-test results, auto-deploy proof, and the D-07 deviation rationale — providing the academic report's evidence trail for DEPLOY-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's browser → Render Dashboard | Authenticated session sets env vars; secrets entered by hand. |
| Operator's browser → Cloudflare Dashboard | Authenticated session sets VITE_API_URL. |
| Public internet → Render service | Anonymous traffic hits FastAPI; CORS gates browser-origin requests. |
| Public internet → CF Pages | Anonymous traffic served from CDN edge. |
| CF Pages SPA → Render API | Cross-origin browser requests; CORS_ORIGINS allow-list controls who can talk to backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | CORS_ORIGINS misformatted | mitigate | Task 4 explicitly demands JSON array format `["https://...pages.dev"]`. Wrong format = pydantic parse fails or wildcard interpretation. Verify in resume signal that login from CF Pages succeeded (proves CORS is correctly narrow). |
| T-07-08 | Information Disclosure | Operator pasting wrong env var values | mitigate | Each env var has its source documented in user_setup. SUMMARY task explicitly forbids recording SECRET_KEY or SMTP_PASS in SUMMARY (acceptance_criteria grep enforces). |
| T-07-09 | Tampering | Fork or rogue PR triggering deploy | accept | Render and CF Pages both deploy from master only (autoDeploy: true on master branch). Forks cannot push to master. Acceptable for academic prototype. |
| T-07-10 | Repudiation | No audit log of who changed env vars | accept | Render and CF dashboards keep their own change logs; not a project concern for prototype scope. |
| T-07-11 | Information Disclosure | Operator's session tokens in browser | mitigate | Tasks instruct operator to perform actions in their own browser session. Claude never sees Render or CF credentials. SUMMARY acceptance_criteria forbids hash/password content. |
| T-07-12 | Denial of Service | Misconfigured _redirects causing infinite loop | mitigate | Plan 01 acceptance_criteria already verified the rule is `200` (rewrite, not 30x redirect). Task 3 step 12 manually verifies SPA fallback works without 404 storm. |
</threat_model>

<verification>
After all six tasks complete:
- Backend reachable at BACKEND_URL/health (200, JSON {status: ok, environment: production}).
- Frontend reachable at FRONTEND_URL/login (renders without console errors).
- Cross-origin login works from FRONTEND_URL to BACKEND_URL for all three profiles using seeded credentials.
- Direct URL `FRONTEND_URL/admin/alunos` does not 404 (SPA fallback proven).
- Verification commit pushed in Task 5 produced a green run in GitHub Actions, a "Live" Render redeploy, and a "Success" CF Pages redeploy.
- 07-02-SUMMARY.md exists and captures all live values without leaking secrets.
- No edits to backend or frontend source code — all production-readiness was already complete (config.py, main.py, api.ts unchanged).
</verification>

<success_criteria>
ROADMAP §Phase 7 success criteria — all four met:
1. Backend FastAPI accessible at a public Render URL — proven by `curl $BACKEND_URL/health` returning 200.
2. Frontend React accessible at a public CF Pages URL — proven by browser load + login flow.
3. Push to master triggers auto-deploy via GitHub Actions (and native webhooks) — proven by Task 5 trigger commit.
4. Env vars (SECRET_KEY, DATABASE_URL, etc.) configured in Render dashboard, not committed — proven by Task 1 acceptance + Plan 01 grep checks on render.yaml.

DEPLOY-01 — Closed.
</success_criteria>

<output>
After completion: `.planning/phases/07-deploy/07-02-SUMMARY.md` already produced by Task 6. The orchestrator's post-phase routine commits the new SUMMARY together with any STATE.md / ROADMAP.md updates.
</output>
</content>
</invoke>